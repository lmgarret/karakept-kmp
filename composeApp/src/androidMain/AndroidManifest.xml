<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".KarakeptApp"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="${appName}"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".BookmarkSavingActivity"
            android:exported="true"
            android:label="${appName}"
            android:launchMode="singleTask"
            android:theme="@style/Theme.App">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QuickShareActivity"
            android:exported="true"
            android:label="${appName} (Quick Save)"
            android:theme="@style/Theme.App.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".services.SaveBookmarkRetryReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.karakept.app.action.RETRY_SAVE" />
            </intent-filter>
        </receiver>

        <!--
          Grants other apps temporary read access to files we hand out, so a PDF asset can be
          opened in an external reader and a backup can be shared. FileUtils already referenced
          this authority but it was never declared, so those hand-offs silently fell back.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Required for compose-ui-test createComposeRule() under Robolectric -->
        <activity android:name="androidx.activity.ComponentActivity" />
    </application>

</manifest>
