<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      API 30+ filters which other apps we can see. Without this, anything that inspects
      intent resolution reports "no app available" even when one is installed.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/pdf" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="text/html" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".KarakeptApp"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/karakept_app_name"
        android:theme="@style/Theme.App.Starting">
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!--
          The launcher entry lives on aliases rather than on MainActivity itself, because an
          icon can only be swapped at runtime by enabling one component and disabling another
          and MainActivity has to stay enabled for notification intents to resolve. Exactly one
          alias is ever enabled; AppIconManager flips the pair. Keep DEFAULT enabled here so a
          fresh install has a launcher entry before any code runs.
        -->
        <activity-alias
            android:name=".LauncherDefault"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher"
            android:label="@string/karakept_app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".LauncherMonochrome"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false"
            android:icon="@mipmap/ic_launcher_mono"
            android:roundIcon="@mipmap/ic_launcher_mono"
            android:label="@string/karakept_app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".BookmarkSavingActivity"
            android:exported="true"
            android:label="@string/karakept_app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.App">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QuickShareActivity"
            android:exported="true"
            android:label="@string/karakept_quick_save_label"
            android:theme="@style/Theme.App.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".services.SaveBookmarkRetryReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.karakept.app.action.RETRY_SAVE" />
            </intent-filter>
        </receiver>

        <!--
          Grants other apps temporary read access to files we hand out, so a PDF asset can be
          opened in an external reader and a backup can be shared. FileUtils already referenced
          this authority but it was never declared, so those hand-offs silently fell back.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Required for compose-ui-test createComposeRule() under Robolectric -->
        <activity android:name="androidx.activity.ComponentActivity" />
    </application>

</manifest>
