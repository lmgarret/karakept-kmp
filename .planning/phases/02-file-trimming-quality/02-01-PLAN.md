---
phase: 02-file-trimming-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/App.kt
autonomous: true
requirements:
  - SIZE-01
  - SIZE-02
  - QUAL-01

must_haves:
  truths:
    - "BookmarkSyncPipeline.kt is under 500 lines"
    - "SettingsRepositoryMutations.kt is under 500 lines"
    - "App.kt has exactly one koinInject<ServerRepository>() call"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/App.kt"
      provides: "Single ServerRepository injection"
      contains: "koinInject<com.karakept.app.data.repository.ServerRepository>"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt"
      provides: "Sync pipeline under 500 lines"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt"
      provides: "Settings mutations under 500 lines"
  key_links:
    - from: "App.kt line 31 serverRepository"
      to: "App.kt line 115 serverRepository.hasServers()"
      via: "lexical scope — outer val used in inner lambda"
      pattern: "val serverRepository.*koinInject"
---

<objective>
Verify file size targets are met (SIZE-01, SIZE-02) and remove the redundant koinInject<ServerRepository>() call in App.kt (QUAL-01).

Purpose: Close out the v1.7.0 tech debt milestone with all three Phase 2 requirements satisfied.
Output: App.kt with a single ServerRepository injection; confirmation that both large files remain under 500 lines.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-trimming-quality/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant koinInject ServerRepository call in App.kt</name>
  <files>composeApp/src/commonMain/kotlin/App.kt</files>
  <read_first>composeApp/src/commonMain/kotlin/App.kt (lines 25-135)</read_first>
  <action>
In App.kt, there are two `koinInject<ServerRepository>()` calls:

- Line 31: `val serverRepository = org.koin.compose.koinInject<com.karakept.app.data.repository.ServerRepository>()`
  Used at line 45 inside the image loader interceptor (`serverRepository.servers.first()`)
- Line 107: `val serverRepository = org.koin.compose.koinInject<com.karakept.app.data.repository.ServerRepository>()`
  Used at line 115 (`serverRepository.hasServers()`)

The line-31 declaration is at the top of the `App()` composable function and is in scope everywhere inside it, including the `AppTheme { }` lambda where line 107 lives. The line-107 declaration shadows the outer variable unnecessarily.

**Fix:** Delete line 107 entirely (`val serverRepository = org.koin.compose.koinInject<com.karakept.app.data.repository.ServerRepository>()`). The inner usage at line 115 (`serverRepository.hasServers()`) will then resolve to the outer variable from line 31, which is correct.

Do NOT move or modify line 31. Do NOT change any other code. This is a single-line deletion.
  </action>
  <verify>
    <automated>grep -c "koinInject&lt;com.karakept.app.data.repository.ServerRepository&gt;" composeApp/src/commonMain/kotlin/App.kt | grep -q "^1$" &amp;&amp; echo "PASS: exactly 1 ServerRepository inject" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "koinInject.*ServerRepository" composeApp/src/commonMain/kotlin/App.kt` returns exactly `1`
- The remaining call is at the top of the `App()` function (near line 31), not inside the `AppTheme` block
- `serverRepository.hasServers()` still exists in the file (the usage, not the declaration)
  </acceptance_criteria>
  <done>App.kt has exactly one koinInject ServerRepository call; the inner usage resolves to the outer variable via lexical scope.</done>
</task>

<task type="auto">
  <name>Task 2: Verify file size targets (SIZE-01, SIZE-02)</name>
  <files>composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt, composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt</files>
  <read_first>No file reading needed — this is a verification-only task</read_first>
  <action>
Run `wc -l` on both files to confirm they are under 500 lines. These targets were already met after Phase 1's println cleanup:
- BookmarkSyncPipeline.kt: expected ~493 lines (SIZE-01)
- SettingsRepositoryMutations.kt: expected ~478 lines (SIZE-02)

No code changes needed. If either file is at or above 500 lines (unexpected), report it but do not modify the files — that would be a separate concern.
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt</automated>
  </verify>
  <acceptance_criteria>
- BookmarkSyncPipeline.kt line count is less than 500
- SettingsRepositoryMutations.kt line count is less than 500
  </acceptance_criteria>
  <done>Both files confirmed under 500 lines. SIZE-01 and SIZE-02 requirements verified as satisfied.</done>
</task>

</tasks>

<verification>
All three Phase 2 requirements verified:
1. SIZE-01: BookmarkSyncPipeline.kt under 500 lines (wc -l confirms)
2. SIZE-02: SettingsRepositoryMutations.kt under 500 lines (wc -l confirms)
3. QUAL-01: App.kt has exactly one koinInject ServerRepository call (grep confirms)
</verification>

<success_criteria>
- `grep -c "koinInject.*ServerRepository" composeApp/src/commonMain/kotlin/App.kt` returns `1`
- `wc -l` on BookmarkSyncPipeline.kt shows < 500
- `wc -l` on SettingsRepositoryMutations.kt shows < 500
- App still compiles (user runs build externally)
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-trimming-quality/02-01-SUMMARY.md`
</output>
