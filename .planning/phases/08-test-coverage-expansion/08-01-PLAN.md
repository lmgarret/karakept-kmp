---
phase: 08-test-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonTest/kotlin/com/karakept/app/utils/ReadingTimeCalculatorTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlSanitizerTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/utils/DateUtilsTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/utils/FaviconUtilsTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/utils/AssetUrlUtilsTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/reader/HighlightOffsetFinderTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/data/model/ListSyncConfigTest.kt
autonomous: true
requirements:
  - COV-01
  - COV-02
  - COV-03

must_haves:
  truths:
    - "All pure utility functions have unit tests exercising their core logic and edge cases"
    - "ReadingTimeCalculator tests cover null/empty input, plain text, HTML with images, and formatting"
    - "HtmlSanitizer tests cover null input, XSS prevention, safe tag preservation, removeFirstImage, and URL validation"
    - "DateUtils tests cover both ELAPSED and ABSOLUTE modes"
    - "FaviconUtils tests cover valid URLs, malformed URLs, and edge cases"
    - "AssetUrlUtils tests cover URL construction and asset URL detection"
    - "HighlightOffsetFinder tests cover simple text match, whitespace normalization, HTML text node walking, and not-found cases"
    - "ListSyncConfig tests cover checkbox state cycling, effective sync list computation with descendants, and parent-in-with-children-mode detection"
  artifacts:
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/utils/ReadingTimeCalculatorTest.kt"
      provides: "ReadingTimeCalculator unit tests"
      contains: "@Test"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlSanitizerTest.kt"
      provides: "HtmlSanitizer unit tests"
      contains: "@Test"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/utils/DateUtilsTest.kt"
      provides: "DateUtils unit tests"
      contains: "@Test"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/utils/FaviconUtilsTest.kt"
      provides: "FaviconUtils unit tests"
      contains: "@Test"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/utils/AssetUrlUtilsTest.kt"
      provides: "AssetUrlUtils unit tests"
      contains: "@Test"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/reader/HighlightOffsetFinderTest.kt"
      provides: "HighlightOffsetFinder unit tests"
      contains: "@Test"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/data/model/ListSyncConfigTest.kt"
      provides: "ListSyncConfig + CheckboxState unit tests"
      contains: "@Test"
  key_links:
    - from: "ReadingTimeCalculatorTest"
      to: "ReadingTimeCalculator.calculateReadingTime"
      via: "direct function call"
    - from: "ListSyncConfigTest"
      to: "ListSyncConfig.getEffectiveSyncLists"
      via: "direct method call with KarakeepList fixtures"
---

<objective>
Write comprehensive unit tests for all 7 untested pure utility/model files in commonTest.

Purpose: These are pure functions and model logic with zero external dependencies -- the highest ROI test targets. They establish a test-writing baseline for the phase.
Output: 7 new test files in commonTest covering ~60 new @Test methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-coverage-expansion/08-RESEARCH.md

@composeApp/src/commonTest/kotlin/com/karakept/app/utils/ParsedDocumentCacheTest.kt
@composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlArchiveProcessorTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure utility tests -- ReadingTimeCalculator, HtmlSanitizer, DateUtils, FaviconUtils, AssetUrlUtils</name>
  <files>
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/ReadingTimeCalculatorTest.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlSanitizerTest.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/DateUtilsTest.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/FaviconUtilsTest.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/AssetUrlUtilsTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/ReadingTimeCalculator.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/HtmlSanitizer.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/DateUtils.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/FaviconUtils.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/AssetUrlUtils.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/DateDisplayMode.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/ParsedDocumentCacheTest.kt
  </read_first>
  <action>
    Create 5 test files in `composeApp/src/commonTest/kotlin/com/karakept/app/utils/`. Package: `com.karakept.app.utils`. Use `kotlin.test` annotations (@Test, assertEquals, assertTrue, assertFalse). Follow the style of existing tests in `ParsedDocumentCacheTest.kt`.

    **ReadingTimeCalculatorTest.kt** -- test `ReadingTimeCalculator` object:
    1. `calculateReadingTime_nullContent_returnsZero` -- pass `null`, assert result is 0
    2. `calculateReadingTime_emptyString_returnsZero` -- pass `""`, assert result is 0
    3. `calculateReadingTime_blankString_returnsZero` -- pass `"  "`, assert result is 0
    4. `calculateReadingTime_singleWord_returnsOneMinute` -- pass `"<p>hello</p>"`, assert result is 1 (minimum)
    5. `calculateReadingTime_250Words_returnsOneMinute` -- generate HTML with exactly 250 "word" tokens, assert result is 1
    6. `calculateReadingTime_500Words_returnsTwoMinutes` -- generate HTML with 500 words, assert result is 2
    7. `calculateReadingTime_htmlWithImages_addsImageTime` -- HTML with 1 image and ~250 words, assert result > 1 (image penalty adds 12s)
    8. `calculateReadingTime_onlyImages_returnsOneMinute` -- HTML with 3 `<img>` tags and no text, assert result >= 1
    9. `calculateReadingTime_malformedHtml_returnsZero` -- test exception handling (pass extremely malformed content; should return 0 or valid int, not throw)
    10. `formatReadingTime_positiveMinutes_returnsFormattedString` -- assert `formatReadingTime(5)` == "5mn"
    11. `formatReadingTime_zero_returnsEmptyString` -- assert `formatReadingTime(0)` == ""

    **HtmlSanitizerTest.kt** -- test `HtmlSanitizer` object:
    1. `sanitize_nullInput_returnsEmpty` -- pass `null`, assert result is ""
    2. `sanitize_emptyInput_returnsEmpty` -- pass `""`, assert result is ""
    3. `sanitize_plainText_preservesContent` -- pass `"<p>Hello world</p>"`, assert result contains "Hello world"
    4. `sanitize_scriptTag_removesScript` -- pass `"<p>Text</p><script>alert('xss')</script>"`, assert result does NOT contain "script" or "alert"
    5. `sanitize_onclickHandler_removesEventHandler` -- pass `"<div onclick='alert()'>Click</div>"`, assert result does NOT contain "onclick"
    6. `sanitize_safeTagsPreserved_keepsDivSpanMark` -- pass HTML with `<div>`, `<span>`, `<mark>` tags, assert they survive sanitization
    7. `sanitize_imageTag_preservesSrcAlt` -- pass `"<img src='https://example.com/img.jpg' alt='test'>"`, assert result contains `src` and `alt`
    8. `sanitize_removeFirstImage_removesOnlyFirst` -- pass HTML with 2 images, call `sanitize(html, removeFirstImage = true)`, assert first image gone, second remains
    9. `isValidUrl_httpsUrl_returnsTrue` -- assert `isValidUrl("https://example.com")` is true
    10. `isValidUrl_httpUrl_returnsTrue` -- assert `isValidUrl("http://example.com")` is true
    11. `isValidUrl_dataUrl_returnsTrue` -- assert `isValidUrl("data:image/png;base64,abc")` is true
    12. `isValidUrl_fileUrl_returnsTrue` -- assert `isValidUrl("file://path")` is true
    13. `isValidUrl_javascriptUrl_returnsFalse` -- assert `isValidUrl("javascript:alert(1)")` is false
    14. `isValidUrl_blankUrl_returnsFalse` -- assert `isValidUrl("")` is false

    **DateUtilsTest.kt** -- test `formatBookmarkDate` and `formatAbsolute`:
    1. `formatAbsolute_knownTimestamp_returnsFormattedDate` -- use a fixed epoch millis (e.g., 1704067200000L = 2024-01-01 UTC), call `formatAbsolute()`, assert result matches "2024-01-01"
    2. `formatBookmarkDate_absoluteMode_delegatesToFormatAbsolute` -- call `formatBookmarkDate(epochMillis, DateDisplayMode.ABSOLUTE)`, assert result matches date format pattern `\\d{4}-\\d{2}-\\d{2}`
    3. `formatBookmarkDate_elapsedMode_returnsRelativeString` -- use `Clock.System.now().toEpochMilliseconds() - 60000` (1 minute ago), call with ELAPSED mode, assert result is "1m ago"
    4. `formatBookmarkDate_elapsedMode_justNow` -- use `Clock.System.now().toEpochMilliseconds() - 5000` (5s ago), assert result is "just now"
    5. `formatBookmarkDate_elapsedMode_hoursAgo` -- use timestamp ~2 hours ago, assert result matches pattern `\\dh ago`
    6. `formatBookmarkDate_elapsedMode_daysAgo` -- use timestamp ~3 days ago, assert result matches pattern `\\dd ago`

    **FaviconUtilsTest.kt** -- test `FaviconUtils` object:
    1. `getFaviconUrl_validHttpsUrl_returnsDuckDuckGoUrl` -- pass `"https://example.com/page"`, assert result is `"https://icons.duckduckgo.com/ip3/example.com.ico"`
    2. `getFaviconUrl_validHttpUrl_extractsDomain` -- pass `"http://blog.example.org/post/123"`, assert result contains `blog.example.org`
    3. `getFaviconUrl_urlWithPort_extractsDomain` -- pass `"https://example.com:8080/page"`, assert result contains `example.com`
    4. `getFaviconUrl_invalidUrl_returnsEmpty` -- pass `"not-a-url"`, assert result is `""`

    **AssetUrlUtilsTest.kt** -- test `AssetUrlUtils` object:
    1. `getAssetUrl_constructsCorrectUrl` -- pass `("https://server.com", "asset-123")`, assert result is `"https://server.com/api/v1/assets/asset-123"`
    2. `getAssetUrl_trimsTrailingSlash` -- pass `("https://server.com/", "asset-123")`, assert no double slash
    3. `isAssetUrl_assetPath_returnsTrue` -- pass `"https://server.com/api/v1/assets/abc"`, assert true
    4. `isAssetUrl_nonAssetPath_returnsFalse` -- pass `"https://server.com/page"`, assert false
  </action>
  <verify>
    <automated>./gradlew :composeApp:desktopTest --tests "com.karakept.app.utils.ReadingTimeCalculatorTest" --tests "com.karakept.app.utils.HtmlSanitizerTest" --tests "com.karakept.app.utils.DateUtilsTest" --tests "com.karakept.app.utils.FaviconUtilsTest" --tests "com.karakept.app.utils.AssetUrlUtilsTest"</automated>
  </verify>
  <acceptance_criteria>
    - ReadingTimeCalculatorTest.kt exists in commonTest/kotlin/com/karakept/app/utils/ and contains at least 10 @Test methods
    - HtmlSanitizerTest.kt exists and contains at least 12 @Test methods
    - DateUtilsTest.kt exists and contains at least 4 @Test methods
    - FaviconUtilsTest.kt exists and contains at least 3 @Test methods
    - AssetUrlUtilsTest.kt exists and contains at least 3 @Test methods
    - All test files use package com.karakept.app.utils
    - ./gradlew :composeApp:desktopTest --tests "com.karakept.app.utils.*Test" exits 0
  </acceptance_criteria>
  <done>All 5 utility test files compile and pass. Tests cover null/empty edge cases, core logic, and error handling for each utility.</done>
</task>

<task type="auto">
  <name>Task 2: HighlightOffsetFinder and ListSyncConfig model tests</name>
  <files>
    composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/reader/HighlightOffsetFinderTest.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/ListSyncConfigTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/reader/HighlightOffsetFinder.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/ListSyncConfig.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/domain/ListHierarchyUtilsTest.kt
  </read_first>
  <action>
    Create 2 test files. Use `kotlin.test` annotations. No mocks needed -- these are pure function / model logic tests.

    **HighlightOffsetFinderTest.kt** -- package `com.karakept.app.ui.components.reader`, test `findTextOffsets()`:
    1. `findTextOffsets_blankSearchText_returnsNull` -- pass `html="<p>text</p>"`, `searchText=""`, assert result is null
    2. `findTextOffsets_simpleMatch_returnsCorrectOffsets` -- pass `html="<p>Hello world</p>"`, `searchText="world"`, assert `result != null`, `result.matchedText` contains "world", and `startOffset < endOffset`
    3. `findTextOffsets_notFound_returnsNull` -- pass `html="<p>Hello</p>"`, `searchText="nonexistent"`, assert result is null
    4. `findTextOffsets_whitespaceNormalization_matchesAcrossSpaces` -- pass HTML with multiple spaces or newlines between words, search for normalized text, assert match is found
    5. `findTextOffsets_multipleTextNodes_matchesAcrossNodes` -- pass `html="<p>Hello <b>world</b> here</p>"`, `searchText="world here"`, assert match found spanning across the `<b>` boundary
    6. `findTextOffsets_caseInsensitive_matchesDifferentCase` -- pass `html="<p>Hello World</p>"`, `searchText="hello world"`, assert match found (function uses lowercase comparison)
    7. `findTextOffsets_nbspNormalization_matchesNbsp` -- pass HTML containing `\u00A0` (non-breaking space), search with regular space, assert match found
    8. `findTextOffsets_invalidHtml_returnsNull` -- verify graceful handling of malformed HTML (should not throw)

    **ListSyncConfigTest.kt** -- package `com.karakept.app.data.model`, test `ListSyncConfig` and `CheckboxState`:

    Helper: Create a function `makeList(id: String, parentId: String? = null): KarakeepList` that returns `KarakeepList(id = id, name = "List $id", parentId = parentId)`.

    CheckboxState tests:
    1. `checkboxState_next_uncheckedToParentOnly` -- assert `UNCHECKED.next() == CHECKED_PARENT_ONLY`
    2. `checkboxState_next_parentOnlyToWithChildren` -- assert `CHECKED_PARENT_ONLY.next() == CHECKED_WITH_CHILDREN`
    3. `checkboxState_next_withChildrenToUnchecked` -- assert `CHECKED_WITH_CHILDREN.next() == UNCHECKED`

    ListSyncConfig tests:
    4. `getCheckboxState_selectedWithChildren_returnsCheckedWithChildren` -- config with listId in both selectedLists and withChildrenMode, assert `getCheckboxState(listId) == CHECKED_WITH_CHILDREN`
    5. `getCheckboxState_selectedOnly_returnsCheckedParentOnly` -- config with listId in selectedLists only, assert `getCheckboxState(listId) == CHECKED_PARENT_ONLY`
    6. `getCheckboxState_notSelected_returnsUnchecked` -- assert `getCheckboxState("unknown") == UNCHECKED`
    7. `getEffectiveSyncLists_noChildren_returnsSameAsSelected` -- config with selectedLists={"A"}, withChildrenMode=empty, allLists=[A], assert effective == {"A"}
    8. `getEffectiveSyncLists_withChildrenMode_includesDescendants` -- config with selectedLists={"parent"}, withChildrenMode={"parent"}, allLists=[parent, child1(parent=parent), child2(parent=parent), grandchild(parent=child1)], assert effective contains parent, child1, child2, grandchild
    9. `isParentInWithChildrenMode_parentIsInMode_returnsTrue` -- config with withChildrenMode={"parent"}, allLists=[parent, child(parent=parent)], call `isParentInWithChildrenMode("parent", allLists)`, assert true
    10. `isParentInWithChildrenMode_grandparentIsInMode_returnsTrue` -- config with withChildrenMode={"grandparent"}, allLists=[grandparent, parent(parent=grandparent), child(parent=parent)], call `isParentInWithChildrenMode("parent", allLists)`, assert true
    11. `isParentInWithChildrenMode_noAncestorInMode_returnsFalse` -- empty withChildrenMode, assert false
    12. `getVisibleLists_rootListSelected_isVisible` -- config with selectedLists={"A"}, allLists=[A(parentId=null)], assert getVisibleLists returns [A]
    13. `getVisibleLists_childSelectedButParentNot_isNotVisible` -- config with selectedLists={"child"}, allLists=[parent(null), child(parent=parent)], assert getVisibleLists is empty (parent not selected)
  </action>
  <verify>
    <automated>./gradlew :composeApp:desktopTest --tests "com.karakept.app.ui.components.reader.HighlightOffsetFinderTest" --tests "com.karakept.app.data.model.ListSyncConfigTest"</automated>
  </verify>
  <acceptance_criteria>
    - HighlightOffsetFinderTest.kt exists in commonTest/kotlin/com/karakept/app/ui/components/reader/ and contains at least 7 @Test methods
    - ListSyncConfigTest.kt exists in commonTest/kotlin/com/karakept/app/data/model/ and contains at least 11 @Test methods
    - Both files use kotlin.test annotations, no mockk imports
    - ListSyncConfigTest.kt imports com.karakept.api.model.KarakeepList
    - ./gradlew :composeApp:desktopTest --tests "com.karakept.app.ui.components.reader.HighlightOffsetFinderTest" exits 0
    - ./gradlew :composeApp:desktopTest --tests "com.karakept.app.data.model.ListSyncConfigTest" exits 0
  </acceptance_criteria>
  <done>HighlightOffsetFinder and ListSyncConfig both have comprehensive test coverage. Tests exercise text offset finding with whitespace normalization and HTML node walking, plus recursive list hierarchy sync configuration logic.</done>
</task>

</tasks>

<verification>
Run the full utility test suite to confirm no regressions:
```bash
./gradlew :composeApp:desktopTest --tests "com.karakept.app.utils.*" --tests "com.karakept.app.ui.components.reader.HighlightOffsetFinderTest" --tests "com.karakept.app.data.model.ListSyncConfigTest"
```
All new tests pass. No existing tests broken.
</verification>

<success_criteria>
- 7 new test files created in commonTest
- At least 55 new @Test methods total across all files
- All tests pass via ./gradlew :composeApp:desktopTest
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-coverage-expansion/08-01-SUMMARY.md`
</output>
