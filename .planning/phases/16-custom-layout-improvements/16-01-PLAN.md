---
phase: 16-custom-layout-improvements
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/model/LayoutType.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/model/BookmarkLayout.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/model/UrlPosition.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/model/UrlDisplayMode.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/model/DescriptionPosition.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/utils/UrlUtils.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/data/model/LayoutTypeTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/data/model/BookmarkLayoutTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/data/model/UrlPositionTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/data/model/UrlDisplayModeTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/data/model/DescriptionPositionTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/ui/utils/UrlUtilsTest.kt
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "LayoutType.fromString('COMPACT_LIST') returns LIST (per D-01, D-02)"
    - "BookmarkLayout has showDescription, descriptionPosition, showUrl, urlDisplayMode, urlPosition fields with backward-compatible defaults"
    - "BUILTIN_COMPACT preset is layoutType LIST with showDescription=false, showTags=false, thumbnailSize=48 (per D-02)"
    - "UrlPosition, UrlDisplayMode, DescriptionPosition enums exist with fromString() that handles unknown values gracefully"
    - "extractDomain strips scheme and www prefix, handles edge cases"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/model/UrlPosition.kt"
      provides: "URL position enum"
      contains: "enum class UrlPosition"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/model/UrlDisplayMode.kt"
      provides: "URL display mode enum"
      contains: "enum class UrlDisplayMode"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/model/DescriptionPosition.kt"
      provides: "Description position enum"
      contains: "enum class DescriptionPosition"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/utils/UrlUtils.kt"
      provides: "Domain extraction utility"
      contains: "fun extractDomain"
  key_links:
    - from: "composeApp/src/commonMain/kotlin/com/karakept/app/data/model/LayoutType.kt"
      to: "LayoutType.LIST"
      via: "fromString('COMPACT_LIST') maps to LIST"
      pattern: "COMPACT_LIST.*->.*LIST"
    - from: "composeApp/src/commonMain/kotlin/com/karakept/app/data/model/BookmarkLayout.kt"
      to: "New enums"
      via: "String-serialized enum fields with defaults"
      pattern: "val showDescription.*Boolean.*=.*true"
---

<objective>
Create the data model foundation for the custom layout improvements: new enums, new BookmarkLayout fields, COMPACT_LIST migration mapping, and URL domain extraction utility. All changes are TDD -- tests written first, then minimal implementation.

Purpose: Every subsequent plan depends on these model types and fields existing. This is the contracts-first wave.
Output: 3 new enum files, updated BookmarkLayout and LayoutType, URL utility, and 6 test files all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-custom-layout-improvements/16-CONTEXT.md
@.planning/phases/16-custom-layout-improvements/16-RESEARCH.md
@.planning/phases/16-custom-layout-improvements/16-VALIDATION.md

<interfaces>
<!-- Existing enum patterns to follow exactly -->

From data/model/MetadataPosition.kt:
```kotlin
package com.karakept.app.data.model

enum class MetadataPosition {
    BELOW, BESIDE, ABOVE;

    companion object {
        fun fromString(value: String): MetadataPosition =
            entries.find { it.name == value } ?: BELOW
    }
}
```

From data/model/BookmarkLayout.kt (current state):
```kotlin
@Serializable
data class BookmarkLayout(
    val id: String,
    val name: String,
    val description: String? = null,
    val icon: String? = null,
    val layoutType: String = LayoutType.LIST.name,
    val showReadingTime: Boolean = true,
    val showDate: Boolean = true,
    val showTags: Boolean = true,
    val dimReadBookmarks: Boolean = true,
    val dateDisplayMode: String = DateDisplayMode.ELAPSED.name,
    val thumbnailSide: String = ThumbnailSide.LEFT.name,
    val showFavicon: Boolean = true,
    val thumbnailSize: Int = 80,
    val metadataPosition: String = MetadataPosition.BELOW.name,
    val tagsScrollable: Boolean = false,
    val quickActionPosition: String = QuickActionPosition.RIGHT.name,
    val isBuiltIn: Boolean = false
)
```

From data/model/LayoutType.kt (current state):
```kotlin
enum class LayoutType {
    CARD, LIST, COMPACT_LIST;

    companion object {
        fun fromString(value: String): LayoutType {
            return when (value.uppercase()) {
                "CARD" -> CARD
                "LIST" -> LIST
                "COMPACT_LIST" -> COMPACT_LIST
                else -> LIST
            }
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New enums + LayoutType migration + URL utility (TDD)</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/UrlPosition.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/UrlDisplayMode.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/DescriptionPosition.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/LayoutType.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/utils/UrlUtils.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/LayoutTypeTest.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/UrlPositionTest.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/UrlDisplayModeTest.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/DescriptionPositionTest.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/ui/utils/UrlUtilsTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/LayoutType.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/MetadataPosition.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/DateDisplayMode.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/BackupSettingsSerializationTest.kt
  </read_first>
  <behavior>
    LayoutTypeTest:
    - fromString("COMPACT_LIST") returns LayoutType.LIST (per D-01, D-02 migration)
    - fromString("LIST") returns LayoutType.LIST
    - fromString("CARD") returns LayoutType.CARD
    - fromString("unknown") returns LayoutType.LIST (default)
    - COMPACT_LIST enum value still exists (for @Deprecated annotation) but fromString never returns it

    UrlPositionTest:
    - fromString("BELOW_TITLE") returns UrlPosition.BELOW_TITLE
    - fromString("METADATA_ROW") returns UrlPosition.METADATA_ROW
    - fromString("INVALID") returns UrlPosition.BELOW_TITLE (default)

    UrlDisplayModeTest:
    - fromString("DOMAIN_ONLY") returns UrlDisplayMode.DOMAIN_ONLY
    - fromString("FULL_URL") returns UrlDisplayMode.FULL_URL
    - fromString("INVALID") returns UrlDisplayMode.DOMAIN_ONLY (default)

    DescriptionPositionTest:
    - fromString("BELOW_TITLE") returns DescriptionPosition.BELOW_TITLE
    - fromString("ABOVE_METADATA") returns DescriptionPosition.ABOVE_METADATA
    - fromString("INVALID") returns DescriptionPosition.BELOW_TITLE (default)

    UrlUtilsTest:
    - extractDomain("https://www.github.com/repo") returns "github.com"
    - extractDomain("https://github.com/repo") returns "github.com"
    - extractDomain("http://example.com:8080/path") returns "example.com"
    - extractDomain("not-a-url") returns "not-a-url" (fallback)
    - extractDomain("") returns "" (empty string)
    - extractDomain("https://sub.domain.co.uk/page") returns "sub.domain.co.uk"
  </behavior>
  <action>
    RED phase -- write all 5 test files first:

    1. Create `LayoutTypeTest.kt` in `composeApp/src/commonTest/kotlin/com/karakept/app/data/model/` with tests for the COMPACT_LIST->LIST migration mapping. Package: `com.karakept.app.data.model`. Use `kotlin.test.Test` and `kotlin.test.assertEquals`.

    2. Create `UrlPositionTest.kt`, `UrlDisplayModeTest.kt`, `DescriptionPositionTest.kt` in the same test package. Each tests `fromString()` for all enum values plus invalid input.

    3. Create `UrlUtilsTest.kt` in `composeApp/src/commonTest/kotlin/com/karakept/app/ui/utils/` testing `extractDomain()`.

    Run tests -- all MUST fail (classes don't exist yet).

    GREEN phase -- create minimal implementations:

    4. Create `UrlPosition.kt` in `com.karakept.app.data.model`:
    ```kotlin
    enum class UrlPosition {
        BELOW_TITLE, METADATA_ROW;
        companion object {
            fun fromString(value: String): UrlPosition =
                entries.find { it.name == value } ?: BELOW_TITLE
        }
    }
    ```

    5. Create `UrlDisplayMode.kt` in `com.karakept.app.data.model`:
    ```kotlin
    enum class UrlDisplayMode {
        DOMAIN_ONLY, FULL_URL;
        companion object {
            fun fromString(value: String): UrlDisplayMode =
                entries.find { it.name == value } ?: DOMAIN_ONLY
        }
    }
    ```

    6. Create `DescriptionPosition.kt` in `com.karakept.app.data.model`:
    ```kotlin
    enum class DescriptionPosition {
        BELOW_TITLE, ABOVE_METADATA;
        companion object {
            fun fromString(value: String): DescriptionPosition =
                entries.find { it.name == value } ?: BELOW_TITLE
        }
    }
    ```

    7. Update `LayoutType.kt`: Add `@Deprecated("Merged into LIST. Kept for deserialization compatibility.")` to `COMPACT_LIST` value. Update `fromString()` to map `"COMPACT_LIST" -> LIST` (per D-01, D-02).

    8. Create `UrlUtils.kt` in `com.karakept.app.ui.utils`:
    ```kotlin
    package com.karakept.app.ui.utils

    fun extractDomain(url: String): String {
        if (url.isBlank()) return url
        return try {
            val uri = java.net.URI(url)
            val host = uri.host ?: return url
            if (host.startsWith("www.")) host.removePrefix("www.") else host
        } catch (_: Exception) {
            url
        }
    }
    ```

    NOTE: `java.net.URI` is available in KMP common code targeting JVM/Android/Desktop. If it causes issues on other targets, use manual parsing as fallback. Check the existing `FaviconUtils` for pattern reference.

    Run tests -- all MUST pass.
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home ./gradlew :composeApp:desktopTest --tests "com.karakept.app.data.model.LayoutTypeTest" --tests "com.karakept.app.data.model.UrlPositionTest" --tests "com.karakept.app.data.model.UrlDisplayModeTest" --tests "com.karakept.app.data.model.DescriptionPositionTest" --tests "com.karakept.app.ui.utils.UrlUtilsTest"</automated>
  </verify>
  <acceptance_criteria>
    - LayoutType.kt contains `"COMPACT_LIST" -> LIST` in the `fromString` when block
    - LayoutType.kt contains `@Deprecated` annotation on `COMPACT_LIST`
    - UrlPosition.kt contains `enum class UrlPosition` with values `BELOW_TITLE, METADATA_ROW`
    - UrlDisplayMode.kt contains `enum class UrlDisplayMode` with values `DOMAIN_ONLY, FULL_URL`
    - DescriptionPosition.kt contains `enum class DescriptionPosition` with values `BELOW_TITLE, ABOVE_METADATA`
    - UrlUtils.kt contains `fun extractDomain(url: String): String`
    - All 5 test files exist and pass
  </acceptance_criteria>
  <done>All 3 new enum files created, LayoutType migration maps COMPACT_LIST to LIST, extractDomain utility works, all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BookmarkLayout new fields + BUILTIN_COMPACT migration (TDD)</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/BookmarkLayout.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/BookmarkLayoutTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/BookmarkLayout.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/data/model/BackupSettingsSerializationTest.kt
  </read_first>
  <behavior>
    BookmarkLayoutTest:
    - Default BookmarkLayout has showDescription=true, descriptionPosition="BELOW_TITLE", showUrl=false, urlDisplayMode="DOMAIN_ONLY", urlPosition="BELOW_TITLE"
    - BUILTIN_COMPACT has layoutType="LIST" (not "COMPACT_LIST"), showDescription=false, showTags=false, thumbnailSize=48, metadataPosition="BESIDE"
    - BUILTIN_LIST has showDescription=true (default), showUrl=false (default)
    - BUILTIN_CARD has showDescription=true (default), showUrl=false (default)
    - Serialization round-trip: BookmarkLayout with new fields serializes and deserializes correctly
    - Backward compat: JSON string missing new fields deserializes with defaults (simulate old stored layout)
  </behavior>
  <action>
    RED phase -- write tests first:

    1. Create `BookmarkLayoutTest.kt` in `composeApp/src/commonTest/kotlin/com/karakept/app/data/model/`. Package: `com.karakept.app.data.model`. Import `kotlinx.serialization.json.Json`, `kotlinx.serialization.encodeToString`, `kotlinx.serialization.decodeFromString`.

    Test cases:
    - `testDefaultFieldValues`: Create `BookmarkLayout(id = "test", name = "Test")` and assert `showDescription == true`, `descriptionPosition == DescriptionPosition.BELOW_TITLE.name`, `showUrl == false`, `urlDisplayMode == UrlDisplayMode.DOMAIN_ONLY.name`, `urlPosition == UrlPosition.BELOW_TITLE.name`.
    - `testBuiltinCompactIsListType`: Assert `BookmarkLayout.BUILTIN_COMPACT.layoutType == LayoutType.LIST.name`, `.showDescription == false`, `.showTags == false`, `.thumbnailSize == 48`, `.metadataPosition == MetadataPosition.BESIDE.name`.
    - `testBuiltinListDefaults`: Assert `BookmarkLayout.BUILTIN_LIST.showDescription == true`, `.showUrl == false`.
    - `testBuiltinCardDefaults`: Assert `BookmarkLayout.BUILTIN_CARD.showDescription == true`, `.showUrl == false`.
    - `testSerializationRoundTrip`: Create a BookmarkLayout with non-default values for all new fields, serialize to JSON string via `Json.encodeToString()`, deserialize back, assert equality.
    - `testBackwardCompatDeserialization`: Create a JSON string manually that only contains `{"id":"old","name":"Old Layout"}` (missing all new fields), deserialize via `Json { ignoreUnknownKeys = true }`, assert new fields have correct defaults.

    Run tests -- MUST fail (fields don't exist).

    GREEN phase:

    2. Update `BookmarkLayout.kt`:
    - Add 5 new fields AFTER `quickActionPosition` and BEFORE `isBuiltIn`:
      ```kotlin
      val showDescription: Boolean = true,
      val descriptionPosition: String = DescriptionPosition.BELOW_TITLE.name,
      val showUrl: Boolean = false,
      val urlDisplayMode: String = UrlDisplayMode.DOMAIN_ONLY.name,
      val urlPosition: String = UrlPosition.BELOW_TITLE.name,
      ```
    - Add imports for DescriptionPosition, UrlPosition, UrlDisplayMode
    - Update `BUILTIN_COMPACT`:
      - Change `layoutType` from `LayoutType.COMPACT_LIST.name` to `LayoutType.LIST.name`
      - Add `showDescription = false`
      - Keep existing: `showTags = false`, `thumbnailSize = 48`, `metadataPosition = MetadataPosition.BESIDE.name`

    Run tests -- all MUST pass.
  </action>
  <verify>
    <automated>JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home ./gradlew :composeApp:desktopTest --tests "com.karakept.app.data.model.BookmarkLayoutTest"</automated>
  </verify>
  <acceptance_criteria>
    - BookmarkLayout.kt contains `val showDescription: Boolean = true`
    - BookmarkLayout.kt contains `val descriptionPosition: String = DescriptionPosition.BELOW_TITLE.name`
    - BookmarkLayout.kt contains `val showUrl: Boolean = false`
    - BookmarkLayout.kt contains `val urlDisplayMode: String = UrlDisplayMode.DOMAIN_ONLY.name`
    - BookmarkLayout.kt contains `val urlPosition: String = UrlPosition.BELOW_TITLE.name`
    - BUILTIN_COMPACT block contains `layoutType = LayoutType.LIST.name`
    - BUILTIN_COMPACT block contains `showDescription = false`
    - BookmarkLayoutTest.kt exists and all tests pass
  </acceptance_criteria>
  <done>BookmarkLayout has 5 new fields with backward-compatible defaults. BUILTIN_COMPACT is now LIST type with compact toggles. Serialization round-trip and backward compat verified.</done>
</task>

</tasks>

<verification>
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home ./gradlew :composeApp:desktopTest --tests "com.karakept.app.data.model.*" --tests "com.karakept.app.ui.utils.UrlUtilsTest"
</verification>

<success_criteria>
- 3 new enum files exist with fromString() methods
- LayoutType.fromString("COMPACT_LIST") returns LIST
- BookmarkLayout has 5 new fields with defaults
- BUILTIN_COMPACT is LIST type with compact defaults
- extractDomain utility handles all URL edge cases
- All 6 test files pass
- Full test suite still green (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/16-custom-layout-improvements/16-01-SUMMARY.md`
</output>
