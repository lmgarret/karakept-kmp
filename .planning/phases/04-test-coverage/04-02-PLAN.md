---
phase: 04-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/desktopTest/kotlin/com/karakept/app/data/model/FilterConfigTest.kt
  - composeApp/src/desktopTest/kotlin/com/karakept/app/ui/screens/BookmarkViewerProgressTest.kt
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "FilterConfig with multiple lists does not crash"
    - "All FilterStatus values combined with varying tag/list cardinalities work correctly"
    - "serverProgressChecked is only true after pullReadingProgressFromServer completes"
    - "Rapid queueReadingProgressUpdate calls deduplicate"
  artifacts:
    - path: "composeApp/src/desktopTest/kotlin/com/karakept/app/data/model/FilterConfigTest.kt"
      provides: "Exhaustive FilterConfig combination tests"
      min_lines: 40
    - path: "composeApp/src/desktopTest/kotlin/com/karakept/app/ui/screens/BookmarkViewerProgressTest.kt"
      provides: "Reading progress race condition tests"
      min_lines: 40
  key_links:
    - from: "FilterConfigTest.kt"
      to: "FilterConfig.kt"
      via: "Tests data class combinations"
      pattern: "FilterConfig"
    - from: "BookmarkViewerProgressTest.kt"
      to: "BookmarkViewerScreenModel.kt"
      via: "Tests serverProgressChecked flag ordering"
      pattern: "serverProgressChecked"
---

<objective>
Add exhaustive FilterConfig combination tests and reading progress race condition tests.

Purpose: TEST-02 covers a previously-crashing multi-list case; TEST-03 covers the race between rapid UI progress updates and server sync.
Output: FilterConfigTest.kt and BookmarkViewerProgressTest.kt with comprehensive test coverage.
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-coverage/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FilterConfigTest with exhaustive combination tests</name>
  <files>composeApp/src/desktopTest/kotlin/com/karakept/app/data/model/FilterConfigTest.kt</files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/model/FilterConfig.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelPagination.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt
  </read_first>
  <behavior>
    - Test 1: FilterConfig with empty lists and empty tags — valid, default state
    - Test 2: FilterConfig with single list — works for all FilterStatus values
    - Test 3: FilterConfig with multiple lists (the crash case) — does not throw for any FilterStatus
    - Test 4: FilterConfig with multiple tags — works correctly
    - Test 5: All SortOption values produce valid FilterConfig
    - Test 6: FilterConfig serialization round-trip preserves all fields (since it is @Serializable)
    - Test 7: FilterConfig equality — same values are equal, different values are not
  </behavior>
  <action>
Create `FilterConfigTest.kt` in `composeApp/src/desktopTest/kotlin/com/karakept/app/data/model/`.

This is a pure data class test — NO mocks needed.

**Test structure:**
- Import FilterConfig, FilterStatus, SortOption from the data model package
- Each test creates FilterConfig instances and asserts properties

**Test 1 — defaultFilterConfigIsValid:**
- `FilterConfig()` with defaults — verify lists is empty, tags is empty, status has default value

**Test 2 — singleListFilterForAllStatuses:**
- Loop through all `FilterStatus.entries`, create `FilterConfig(status = it, lists = listOf("list-1"))`, verify no exception and lists.size == 1

**Test 3 — multiListFilterDoesNotCrash:**
- Create FilterConfig with `lists = listOf("list-1", "list-2", "list-3")` for each FilterStatus value
- Verify no exception, `config.lists.size == 3`
- This is the critical regression test for the previously-crashing case

**Test 4 — multipleTagsFilterWorks:**
- Create FilterConfig with `tags = listOf("tag-a", "tag-b", "tag-c")`
- Verify tags.size == 3

**Test 5 — allSortOptionsValid:**
- Loop through all `SortOption.entries`, create FilterConfig with each, verify no exception

**Test 6 — serializationRoundTrip:**
- Create a FilterConfig with all fields populated
- Serialize to JSON string with `Json.encodeToString`
- Deserialize back with `Json.decodeFromString`
- assertEquals original and deserialized

**Test 7 — equalityContract:**
- Two identical FilterConfig instances are equal
- Changing any field produces inequality

Also investigate the multi-list crash: read MainScreenModelPagination.kt to understand how FilterConfig.lists is consumed in pagination queries. If the crash was in how multiple list IDs are passed to a query, add a test that exercises that code path (may need to mock the DAO to test the query construction).
  </action>
  <verify>
    <automated>Tell user to run: ./gradlew :composeApp:desktopTest --tests "*.FilterConfigTest"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at composeApp/src/desktopTest/kotlin/com/karakept/app/data/model/FilterConfigTest.kt
    - Contains @Test annotations for at least 6 test methods
    - Contains "listOf" with multiple list IDs (multi-list crash regression)
    - Contains "FilterStatus" iteration or explicit coverage of all status values
    - Contains serialization round-trip test with Json.encodeToString/decodeFromString
  </acceptance_criteria>
  <done>FilterConfigTest passes with exhaustive coverage of status x list x tag combinations, including the multi-list regression case and serialization round-trip</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BookmarkViewerProgressTest for reading progress race condition</name>
  <files>composeApp/src/desktopTest/kotlin/com/karakept/app/ui/screens/BookmarkViewerProgressTest.kt</files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreenModel.kt
    composeApp/src/desktopTest/kotlin/com/karakept/app/data/integration/ReadingProgressIntegrationTest.kt
  </read_first>
  <behavior>
    - Test 1: serverProgressChecked starts as false
    - Test 2: serverProgressChecked becomes true only after pullReadingProgressFromServer completes
    - Test 3: rapid queueReadingProgressUpdate calls deduplicate (only one server call)
    - Test 4: local progress update does not set serverProgressChecked to true
  </behavior>
  <action>
Create `BookmarkViewerProgressTest.kt` in `composeApp/src/desktopTest/kotlin/com/karakept/app/ui/screens/`.

**Setup:**
- Read BookmarkViewerScreenModel constructor to understand required dependencies
- Mock all dependencies with MockK (bookmarkDao, remoteDataSource, settingsRepository, etc.)
- Use `runTest` with `StandardTestDispatcher` or `UnconfinedTestDispatcher`
- If BookmarkViewerScreenModel uses `Dispatchers.IO` internally, may need `Dispatchers.setMain(testDispatcher)` in @Before and `Dispatchers.resetMain()` in @After
- The ScreenModel needs a bookmark ID to initialize — provide a test bookmark ID and mock the DAO to return a test bookmark entity

**Test 1 — serverProgressCheckedStartsFalse:**
- Create ScreenModel instance
- Assert `serverProgressChecked.value == false` immediately

**Test 2 — serverProgressCheckedTrueAfterPull:**
- Mock the pull reading progress call to return a value (or empty)
- Trigger the initialization flow that calls pullReadingProgressFromServer
- `advanceUntilIdle()`
- Assert `serverProgressChecked.value == true`

**Test 3 — rapidProgressUpdatesDeduplicate:**
- Call `queueReadingProgressUpdate(0.1)`, `queueReadingProgressUpdate(0.2)`, `queueReadingProgressUpdate(0.3)` in rapid succession
- `advanceUntilIdle()`
- Verify remoteDataSource update called only once (or a small number, not 3 times) — the deduplication logic should batch/debounce

**Test 4 — localProgressDoesNotSetServerChecked:**
- Call `queueReadingProgressUpdate(0.5)` without server pull completing
- Assert `serverProgressChecked.value == false` — the flag is only for server-to-local sync, not local-to-server

Note: The exact method names and state flow field names must be read from BookmarkViewerScreenModel.kt. Adapt test names and assertions to match the actual API.
  </action>
  <verify>
    <automated>Tell user to run: ./gradlew :composeApp:desktopTest --tests "*.BookmarkViewerProgressTest"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at composeApp/src/desktopTest/kotlin/com/karakept/app/ui/screens/BookmarkViewerProgressTest.kt
    - Contains @Test annotations for at least 3 test methods
    - Contains "serverProgressChecked" assertions
    - Contains "queueReadingProgressUpdate" or equivalent method calls
    - Contains runTest from kotlinx-coroutines-test
    - Contains MockK setup (mockk or relaxed)
  </acceptance_criteria>
  <done>BookmarkViewerProgressTest passes with tests proving serverProgressChecked ordering is correct and rapid updates deduplicate</done>
</task>

</tasks>

<verification>
All tests pass: `./gradlew :composeApp:desktopTest --tests "*.FilterConfigTest"` and `./gradlew :composeApp:desktopTest --tests "*.BookmarkViewerProgressTest"`
</verification>

<success_criteria>
- FilterConfigTest.kt exists with 6+ passing tests covering all FilterStatus values, multi-list regression, serialization
- BookmarkViewerProgressTest.kt exists with 3+ passing tests covering serverProgressChecked ordering and deduplication
- All tests are pure unit tests (no Docker, no integration infrastructure)
- Tests run in seconds
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-coverage/04-02-SUMMARY.md`
</output>
