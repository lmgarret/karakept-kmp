---
phase: 10-dialog-and-component-ui-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagEditorDialog.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/BookmarkTagsDisplay.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/TagEditorLogicTest.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/BookmarkTagsParsingTest.kt
autonomous: true
requirements:
  - TAG-FILTER
  - TAG-CANADD
  - TAG-PARSE

must_haves:
  truths:
    - "TagEditorDialog suggestion filtering logic is tested as a pure function in commonTest"
    - "TagEditorDialog canAdd gate logic is tested as a pure function in commonTest"
    - "BookmarkTagsDisplay tag parsing logic is tested as a pure function in commonTest"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagEditorDialog.kt"
      provides: "Extracted filterTagSuggestions() and canAddTag() internal top-level functions"
      contains: "internal fun filterTagSuggestions"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/BookmarkTagsDisplay.kt"
      provides: "Extracted parseTagString() internal top-level function"
      contains: "internal fun parseTagString"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/TagEditorLogicTest.kt"
      provides: "Tests for filterTagSuggestions and canAddTag"
      min_lines: 60
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/BookmarkTagsParsingTest.kt"
      provides: "Tests for parseTagString"
      min_lines: 30
  key_links:
    - from: "TagEditorLogicTest.kt"
      to: "TagEditorDialog.kt"
      via: "import filterTagSuggestions, canAddTag"
      pattern: "import com.karakept.app.ui.components.filterTagSuggestions"
    - from: "BookmarkTagsParsingTest.kt"
      to: "BookmarkTagsDisplay.kt"
      via: "import parseTagString"
      pattern: "import com.karakept.app.ui.components.parseTagString"
---

<objective>
Extract pure logic from TagEditorDialog and BookmarkTagsDisplay into testable top-level functions, then write comprehensive commonTest unit tests covering suggestion filtering, the canAdd gate, and tag string parsing.

Purpose: The suggestion filtering and canAdd gate in TagEditorDialog, and the tag parsing in BookmarkTagsDisplay, contain the most interesting logic in these components. Extracting and testing them as pure functions gives fast, cross-platform coverage without Robolectric overhead.

Output: Two modified production files with extracted functions, two new test files in commonTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dialog-and-component-ui-tests/10-RESEARCH.md

<interfaces>
<!-- Production files being modified -->

From composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagEditorDialog.kt:
- Lines 63-69: Inline suggestion filtering logic inside `remember` block
- Lines 102-104: Inline `canAdd` computation
- Both must be extracted as `internal fun` at top level (outside the composable)
- The composable must call the extracted functions in place of the inline logic

From composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/BookmarkTagsDisplay.kt:
- Lines 41-43: Inline tag parsing `tags.split(",").map { it.trim() }.filter { it.isNotBlank() }`
- Must be extracted as `internal fun parseTagString(tags: String): List<String>` at top level
- The composable must call `parseTagString(tags)` in place of the inline expression
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract pure functions from TagEditorDialog and BookmarkTagsDisplay</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagEditorDialog.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/BookmarkTagsDisplay.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagEditorDialog.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/BookmarkTagsDisplay.kt
  </read_first>
  <action>
**TagEditorDialog.kt** — Add two `internal` top-level functions ABOVE the `TagEditorDialog` composable:

1. `filterTagSuggestions(availableTags: List&lt;String&gt;, searchInput: String, currentTags: List&lt;String&gt;): List&lt;String&gt;`
   - If `searchInput.isBlank()` return `emptyList()`
   - Filter `availableTags` to those containing `searchInput` (case-insensitive) AND not in `currentTags`
   - Sort by `lowercase()`
   - Take max 8 results
   - This exactly mirrors lines 63-69

2. `canAddTag(searchInput: String, currentTags: List&lt;String&gt;, availableTags: List&lt;String&gt;, canCreateNew: Boolean): Boolean`
   - `val trimmed = searchInput.trim()`
   - If `trimmed.isBlank()` return false
   - If `currentTags.contains(trimmed)` return false
   - If `canCreateNew` return true
   - Otherwise return `availableTags.any { it.equals(trimmed, ignoreCase = true) }`
   - This exactly mirrors lines 102-104

Replace the inline `remember` block (lines 63-69) with a call to `filterTagSuggestions(availableTags, searchInput, tags)`. Replace the inline `canAdd` computation (lines 102-104) with a call to `canAddTag(searchInput, tags, availableTags, canCreateNew)`. Remove the `exactMatch` local variable — it is only needed inside `canAddTag` now, BUT keep the `exactMatch` usage at line 115 (`addTag(exactMatch ?: searchInput)`). To handle this: also extract a helper `findExactTagMatch(searchInput: String, availableTags: List&lt;String&gt;): String?` that returns `availableTags.find { it.equals(searchInput.trim(), ignoreCase = true) }`, and use it at the call site.

**BookmarkTagsDisplay.kt** — Add one `internal` top-level function ABOVE the `BookmarkTagsDisplay` composable:

1. `parseTagString(tags: String): List&lt;String&gt;`
   - `tags.split(",").map { it.trim() }.filter { it.isNotBlank() }`
   - This exactly mirrors lines 41-43

Replace line 41-43 (`val tagList = ...`) with `val tagList = parseTagString(tags)`.

Both files must compile and behave identically to before — these are pure refactors with no behavior change.
  </action>
  <verify>
    <automated>cd /Users/louis-maxence.garret/git/karakept-kmp && JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :composeApp:compileKotlinDesktop 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TagEditorDialog.kt contains `internal fun filterTagSuggestions(`
    - TagEditorDialog.kt contains `internal fun canAddTag(`
    - TagEditorDialog.kt contains `internal fun findExactTagMatch(`
    - TagEditorDialog.kt composable calls `filterTagSuggestions(availableTags, searchInput, tags)` inside the remember block
    - TagEditorDialog.kt composable calls `canAddTag(searchInput, tags, availableTags, canCreateNew)` instead of inline computation
    - BookmarkTagsDisplay.kt contains `internal fun parseTagString(`
    - BookmarkTagsDisplay.kt composable calls `parseTagString(tags)` instead of inline parsing
    - `compileKotlinDesktop` succeeds with no errors
  </acceptance_criteria>
  <done>Three internal top-level functions extracted, composables call them, project compiles</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write TagEditorLogicTest and BookmarkTagsParsingTest in commonTest</name>
  <files>
    composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/TagEditorLogicTest.kt
    composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/BookmarkTagsParsingTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagEditorDialog.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/BookmarkTagsDisplay.kt
  </read_first>
  <behavior>
    TagEditorLogicTest — filterTagSuggestions:
    - Blank search input returns empty list
    - Prefix match returns matching tags case-insensitively (e.g. "kot" matches "Kotlin", "kotlin-kmp")
    - Already-selected tags are excluded from results
    - Results are sorted alphabetically by lowercase
    - Max 8 results returned (test with >8 matching tags)
    - Exact match is included in results when not already selected

    TagEditorLogicTest — canAddTag:
    - canCreateNew=true + non-blank input not in currentTags -> true
    - canCreateNew=false + input matches availableTags case-insensitively -> true
    - canCreateNew=false + input NOT in availableTags -> false
    - Input already in currentTags -> false regardless of canCreateNew
    - Blank input -> false
    - Input with leading/trailing whitespace is trimmed before checks

    TagEditorLogicTest — findExactTagMatch:
    - Returns exact case-insensitive match from availableTags
    - Returns null when no match exists
    - Trims input before matching

    BookmarkTagsParsingTest — parseTagString:
    - "kotlin,android,kmp" produces ["kotlin", "android", "kmp"]
    - "kotlin, android , kmp" (with spaces) produces ["kotlin", "android", "kmp"]
    - "" produces empty list
    - ",," produces empty list
    - "single" produces ["single"]
    - " , tag , " produces ["tag"]
  </behavior>
  <action>
Create `TagEditorLogicTest.kt` in `composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/`:
- Package: `com.karakept.app.ui.components`
- Import `filterTagSuggestions`, `canAddTag`, `findExactTagMatch` from the same package
- Use `kotlin.test.Test` and `kotlin.test.assertEquals` / `kotlin.test.assertTrue` / `kotlin.test.assertFalse` / `kotlin.test.assertNull`
- Write tests for all behaviors listed above

Create `BookmarkTagsParsingTest.kt` in the same directory:
- Package: `com.karakept.app.ui.components`
- Import `parseTagString` from the same package
- Use `kotlin.test.Test` and `kotlin.test.assertEquals`
- Write tests for all parsing cases listed above
  </action>
  <verify>
    <automated>cd /Users/louis-maxence.garret/git/karakept-kmp && JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :composeApp:desktopTest --tests "com.karakept.app.ui.components.TagEditorLogicTest" --tests "com.karakept.app.ui.components.BookmarkTagsParsingTest" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TagEditorLogicTest.kt exists at composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/TagEditorLogicTest.kt
    - TagEditorLogicTest.kt contains `import com.karakept.app.ui.components.filterTagSuggestions`
    - TagEditorLogicTest.kt contains `import com.karakept.app.ui.components.canAddTag`
    - TagEditorLogicTest.kt contains at least 12 @Test functions (6 for filterTagSuggestions, 6 for canAddTag/findExactTagMatch)
    - BookmarkTagsParsingTest.kt exists at composeApp/src/commonTest/kotlin/com/karakept/app/ui/components/BookmarkTagsParsingTest.kt
    - BookmarkTagsParsingTest.kt contains `import com.karakept.app.ui.components.parseTagString`
    - BookmarkTagsParsingTest.kt contains at least 5 @Test functions
    - desktopTest for both test classes passes with 0 failures
  </acceptance_criteria>
  <done>All pure logic tests pass in commonTest — filterTagSuggestions, canAddTag, findExactTagMatch, and parseTagString fully covered</done>
</task>

</tasks>

<verification>
Run the full commonTest suite to confirm no regressions:
```
JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :composeApp:desktopTest 2>&1 | tail -10
```
All tests should pass including the new TagEditorLogicTest and BookmarkTagsParsingTest.
</verification>

<success_criteria>
- filterTagSuggestions, canAddTag, findExactTagMatch extracted from TagEditorDialog.kt as internal top-level functions
- parseTagString extracted from BookmarkTagsDisplay.kt as internal top-level function
- Composables use the extracted functions (no inline logic duplication)
- TagEditorLogicTest covers 12+ test cases for suggestion filtering, canAdd gate, and exact match
- BookmarkTagsParsingTest covers 5+ test cases for tag string parsing
- All desktopTest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-dialog-and-component-ui-tests/10-01-SUMMARY.md`
</output>
