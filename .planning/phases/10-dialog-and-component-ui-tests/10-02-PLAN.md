---
phase: 10-dialog-and-component-ui-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/androidUnitTest/kotlin/com/karakept/app/ui/components/TagChipTest.kt
autonomous: true
requirements:
  - TAG-CHIP-UI

must_haves:
  truths:
    - "TagChip displays the tag text"
    - "TagChip shows remove icon only when onRemove callback is provided"
    - "TagChip fires onRemove callback when remove icon is clicked"
    - "TagChip fires onClick callback when chip is clicked"
    - "TagChip applies selected styling when selected=true"
  artifacts:
    - path: "composeApp/src/androidUnitTest/kotlin/com/karakept/app/ui/components/TagChipTest.kt"
      provides: "Compose UI tests for TagChip render states and callback wiring"
      min_lines: 60
  key_links:
    - from: "TagChipTest.kt"
      to: "TagChip.kt"
      via: "direct composable render in setContent"
      pattern: "TagChip\\(tag ="
---

<objective>
Write Compose UI tests for the TagChip component using Robolectric, covering text display, remove icon visibility, callback wiring for onClick and onRemove, and selected state rendering.

Purpose: TagChip is a core reusable component used throughout the app. Testing its callback wiring and conditional rendering ensures it works correctly when used in TagEditorDialog, BookmarkTagsDisplay, and other consumers.

Output: One new test file in androidUnitTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dialog-and-component-ui-tests/10-RESEARCH.md

<interfaces>
<!-- TagChip composable signature (from TagChip.kt) -->

```kotlin
@Composable
fun TagChip(
    tag: String,
    selected: Boolean = false,
    onRemove: (() -> Unit)? = null,
    onClick: (() -> Unit)? = null,
    modifier: Modifier = Modifier
)
```

Key implementation details for testing:
- Remove button uses `IconButton` with `Icon(contentDescription = "Remove tag")` — find via `onNodeWithContentDescription("Remove tag")`
- Click uses `Modifier.clickable` on the Surface — `onNodeWithText(tag).performClick()` triggers onClick
- Selected state changes container from secondaryContainer to primaryContainer and adds a border
- Tag text rendered as `Text(text = tag)` — find via `onNodeWithText(tag)`

<!-- Existing test pattern (from ScrollPositionRegressionTest.kt) -->
- @RunWith(RobolectricTestRunner::class)
- @Config(sdk = [34], application = android.app.Application::class)
- createComposeRule() for Compose UI tests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write TagChip Compose UI tests</name>
  <files>
    composeApp/src/androidUnitTest/kotlin/com/karakept/app/ui/components/TagChipTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/TagChip.kt
    composeApp/src/androidUnitTest/kotlin/com/karakept/app/ui/screens/ScrollPositionRegressionTest.kt
  </read_first>
  <behavior>
    - Tag text "kotlin" is displayed when TagChip(tag = "kotlin") is rendered
    - Remove icon (contentDescription "Remove tag") exists when onRemove is provided
    - Remove icon does NOT exist when onRemove is null (default)
    - Clicking remove icon fires onRemove callback (var flipped to true)
    - Clicking chip text fires onClick callback (var flipped to true)
    - TagChip renders without crash when both onClick and onRemove are provided
    - TagChip renders without crash when selected=true
  </behavior>
  <action>
Create `TagChipTest.kt` at `composeApp/src/androidUnitTest/kotlin/com/karakept/app/ui/components/TagChipTest.kt`:

- Package: `com.karakept.app.ui.components`
- Annotations: `@RunWith(RobolectricTestRunner::class)`, `@Config(sdk = [34], application = android.app.Application::class)`
- Rule: `@get:Rule val composeTestRule = createComposeRule()`
- All `setContent` blocks must wrap content in `MaterialTheme { ... }` (Pitfall 3)

Tests to write:

1. `displays tag text` — setContent with `TagChip(tag = "kotlin")`, assert `onNodeWithText("kotlin").assertIsDisplayed()`

2. `shows remove icon when onRemove provided` — setContent with `TagChip(tag = "test", onRemove = {})`, assert `onNodeWithContentDescription("Remove tag").assertExists()`

3. `hides remove icon when onRemove is null` — setContent with `TagChip(tag = "test")`, assert `onNodeWithContentDescription("Remove tag").assertDoesNotExist()`

4. `remove button triggers onRemove callback` — var `removed = false`, setContent with `TagChip(tag = "test", onRemove = { removed = true })`, performClick on `onNodeWithContentDescription("Remove tag")`, assert `assertTrue(removed)`

5. `click triggers onClick callback` — var `clicked = false`, setContent with `TagChip(tag = "test", onClick = { clicked = true })`, performClick on `onNodeWithText("test")`, assert `assertTrue(clicked)`

6. `renders with both onClick and onRemove` — setContent with both callbacks, assert tag text displayed and remove icon exists (no crash)

7. `renders in selected state` — setContent with `TagChip(tag = "test", selected = true)`, assert `onNodeWithText("test").assertIsDisplayed()` (no crash, text visible)

Imports needed:
- `org.junit.Rule`, `org.junit.Test`, `org.junit.runner.RunWith`
- `org.robolectric.RobolectricTestRunner`
- `org.robolectric.annotation.Config`
- `androidx.compose.ui.test.junit4.createComposeRule`
- `androidx.compose.ui.test.onNodeWithText`, `onNodeWithContentDescription`
- `androidx.compose.ui.test.assertIsDisplayed`, `assertExists`, `assertDoesNotExist`, `performClick`
- `androidx.compose.material3.MaterialTheme`
- `kotlin.test.assertTrue`
  </action>
  <verify>
    <automated>cd /Users/louis-maxence.garret/git/karakept-kmp && JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :composeApp:testDebugUnitTest --tests "com.karakept.app.ui.components.TagChipTest" 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TagChipTest.kt exists at composeApp/src/androidUnitTest/kotlin/com/karakept/app/ui/components/TagChipTest.kt
    - TagChipTest.kt contains `@RunWith(RobolectricTestRunner::class)`
    - TagChipTest.kt contains `@Config(sdk = [34]`
    - TagChipTest.kt contains `MaterialTheme` wrapping all setContent blocks
    - TagChipTest.kt contains `onNodeWithContentDescription("Remove tag")` for remove icon assertions
    - TagChipTest.kt contains at least 7 @Test functions
    - testDebugUnitTest for TagChipTest passes with 0 failures
  </acceptance_criteria>
  <done>All 7 TagChip UI tests pass — text display, remove icon visibility, onClick callback, onRemove callback, combined callbacks, and selected state all verified</done>
</task>

</tasks>

<verification>
Run the full androidUnitTest suite to confirm no regressions:
```
JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew :composeApp:testDebugUnitTest --tests "com.karakept.app.ui.components.*" 2>&1 | tail -10
```
All TagChipTest tests should pass.
</verification>

<success_criteria>
- TagChipTest.kt exists with 7 tests covering all key behaviors
- All tests use MaterialTheme wrapper
- All tests use Robolectric with SDK 34
- Callback wiring verified for both onClick and onRemove
- Remove icon conditional rendering verified
- Selected state rendering verified (no crash)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-dialog-and-component-ui-tests/10-02-SUMMARY.md`
</output>
