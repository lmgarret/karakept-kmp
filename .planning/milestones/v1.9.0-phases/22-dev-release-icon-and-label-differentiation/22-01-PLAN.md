---
phase: 22-dev-release-icon-and-label-differentiation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/build.gradle.kts
  - composeApp/src/commonMain/kotlin/Platform.kt
  - composeApp/src/androidMain/kotlin/Platform.android.kt
  - composeApp/src/desktopMain/kotlin/Platform.jvm.kt
  - composeApp/src/androidMain/res/values/themes.xml
  - composeApp/src/androidMain/res/values-night/themes.xml
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/OnboardingScreen.kt
  - composeApp/src/devRelease/res/drawable/dev_banner.xml
  - composeApp/src/devRelease/res/drawable/ic_launcher_foreground_dev.xml
  - composeApp/src/devRelease/res/drawable/ic_notification.xml
  - composeApp/src/devRelease/res/mipmap-anydpi-v26/ic_launcher.xml
autonomous: false
requirements:
  - DEV-01
must_haves:
  truths:
    - "devRelease build shows a distinct launcher icon with DEV ribbon overlay"
    - "devRelease build shows DEV badge on OnboardingScreen welcome step"
    - "regular release/debug builds show normal icon with no DEV overlay"
    - "desktop build compiles with isDevBuild = false"
  artifacts:
    - path: "composeApp/src/devRelease/res/drawable/dev_banner.xml"
      provides: "Red diagonal DEV ribbon vector drawable"
    - path: "composeApp/src/devRelease/res/mipmap-anydpi-v26/ic_launcher.xml"
      provides: "Adaptive icon overriding foreground with dev banner overlay"
    - path: "composeApp/src/commonMain/kotlin/Platform.kt"
      provides: "expect val isDevBuild: Boolean declaration"
      contains: "expect val isDevBuild"
    - path: "composeApp/src/androidMain/kotlin/Platform.android.kt"
      provides: "actual isDevBuild reading BuildConfig.IS_DEV"
      contains: "BuildConfig.IS_DEV"
    - path: "composeApp/src/desktopMain/kotlin/Platform.jvm.kt"
      provides: "actual isDevBuild = false for desktop"
      contains: "actual val isDevBuild: Boolean = false"
  key_links:
    - from: "composeApp/build.gradle.kts"
      to: "BuildConfig.IS_DEV"
      via: "buildConfigField in defaultConfig (false) and devRelease flavor (true)"
      pattern: "buildConfigField.*IS_DEV"
    - from: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/OnboardingScreen.kt"
      to: "isDevBuild"
      via: "import isDevBuild; conditional DEV badge rendering"
      pattern: "if \\(isDevBuild\\)"
    - from: "composeApp/src/devRelease/res/mipmap-anydpi-v26/ic_launcher.xml"
      to: "ic_launcher_foreground_dev"
      via: "foreground drawable reference"
      pattern: "ic_launcher_foreground_dev"
---

<objective>
Cherry-pick and validate the draft DEV icon/label implementation from branch origin/claude/add-dev-logo-label-pYLbN.

Purpose: Distinguish dev release installs from production on the same device via a visible "DEV" ribbon on the launcher icon and a "DEV" badge in the onboarding UI (GitHub issue #177).

Output: Working devRelease build variant with distinct icon overlay and in-app DEV badge; all existing tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@composeApp/build.gradle.kts
@composeApp/src/commonMain/kotlin/Platform.kt
@composeApp/src/androidMain/kotlin/Platform.android.kt
@composeApp/src/desktopMain/kotlin/Platform.jvm.kt
@composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/OnboardingScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cherry-pick draft and verify compilation</name>
  <files>
    composeApp/build.gradle.kts,
    composeApp/src/commonMain/kotlin/Platform.kt,
    composeApp/src/androidMain/kotlin/Platform.android.kt,
    composeApp/src/desktopMain/kotlin/Platform.jvm.kt,
    composeApp/src/androidMain/res/values/themes.xml,
    composeApp/src/androidMain/res/values-night/themes.xml,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/OnboardingScreen.kt,
    composeApp/src/devRelease/res/drawable/dev_banner.xml,
    composeApp/src/devRelease/res/drawable/ic_launcher_foreground_dev.xml,
    composeApp/src/devRelease/res/drawable/ic_notification.xml,
    composeApp/src/devRelease/res/mipmap-anydpi-v26/ic_launcher.xml
  </files>
  <read_first>
    composeApp/build.gradle.kts,
    composeApp/src/commonMain/kotlin/Platform.kt,
    composeApp/src/androidMain/kotlin/Platform.android.kt,
    composeApp/src/desktopMain/kotlin/Platform.jvm.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/OnboardingScreen.kt
  </read_first>
  <action>
    1. Cherry-pick commit d5961d1 from origin/claude/add-dev-logo-label-pYLbN:
       ```
       git cherry-pick d5961d1
       ```
       The cherry-pick has been tested and applies cleanly with no conflicts.

    2. If any merge conflict arises (unlikely), resolve by keeping the draft's additions on top of the current state of each file.

    3. Verify the cherry-picked changes include all expected modifications:
       - build.gradle.kts: `buildFeatures { buildConfig = true }` block present, `buildConfigField("boolean", "IS_DEV", "false")` in defaultConfig, `buildConfigField("boolean", "IS_DEV", "true")` in devRelease flavor
       - Platform.kt: `expect val isDevBuild: Boolean` line added
       - Platform.android.kt: `actual val isDevBuild: Boolean = BuildConfig.IS_DEV` added with `import com.karakept.app.BuildConfig`
       - Platform.jvm.kt: `actual val isDevBuild: Boolean = false` added
       - OnboardingScreen.kt: `import isDevBuild` and `if (isDevBuild)` conditional DEV badge in WelcomeStep
       - themes.xml (both): `windowSplashScreenAnimatedIcon` line added
       - 4 new files in composeApp/src/devRelease/res/

    4. Run the full test suite to confirm no regressions:
       ```
       export JAVA_HOME="/opt/homebrew/Cellar/openjdk@21/21.0.10/libexec/openjdk.jdk/Contents/Home"
       export ANDROID_HOME="/opt/homebrew/share/android-commandlinetools"
       ./gradlew :composeApp:testDebugUnitTest
       ```

    5. Build the devRelease APK to confirm the icon overlay and buildConfig compile:
       ```
       export JAVA_HOME="/opt/homebrew/Cellar/openjdk@21/21.0.10/libexec/openjdk.jdk/Contents/Home"
       export ANDROID_HOME="/opt/homebrew/share/android-commandlinetools"
       ./gradlew :composeApp:assembleDevRelease
       ```

    6. Build the desktop target to confirm isDevBuild = false compiles:
       ```
       export JAVA_HOME="/opt/homebrew/Cellar/openjdk@21/21.0.10/libexec/openjdk.jdk/Contents/Home"
       ./gradlew :composeApp:compileKotlinDesktop
       ```
  </action>
  <verify>
    <automated>
      export JAVA_HOME="/opt/homebrew/Cellar/openjdk@21/21.0.10/libexec/openjdk.jdk/Contents/Home" && export ANDROID_HOME="/opt/homebrew/share/android-commandlinetools" && ./gradlew :composeApp:testDebugUnitTest :composeApp:assembleDevRelease :composeApp:compileKotlinDesktop --no-daemon 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows cherry-picked commit message containing "DEV label"
    - composeApp/build.gradle.kts contains `buildConfig = true`
    - composeApp/build.gradle.kts contains `buildConfigField("boolean", "IS_DEV", "false")`
    - composeApp/build.gradle.kts contains `buildConfigField("boolean", "IS_DEV", "true")`
    - composeApp/src/commonMain/kotlin/Platform.kt contains `expect val isDevBuild: Boolean`
    - composeApp/src/androidMain/kotlin/Platform.android.kt contains `BuildConfig.IS_DEV`
    - composeApp/src/desktopMain/kotlin/Platform.jvm.kt contains `actual val isDevBuild: Boolean = false`
    - composeApp/src/devRelease/res/drawable/dev_banner.xml exists and contains `fillColor="#D32F2F"`
    - composeApp/src/devRelease/res/mipmap-anydpi-v26/ic_launcher.xml exists and contains `ic_launcher_foreground_dev`
    - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/OnboardingScreen.kt contains `if (isDevBuild)`
    - `./gradlew :composeApp:testDebugUnitTest` exits 0
    - `./gradlew :composeApp:assembleDevRelease` exits 0 (produces APK)
    - `./gradlew :composeApp:compileKotlinDesktop` exits 0
  </acceptance_criteria>
  <done>Draft cherry-picked, all tests pass, devRelease APK builds, desktop compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify DEV icon on device</name>
  <files>none</files>
  <action>
    Install the devRelease build on the user's phone and have them visually verify:
    1. Run `./gradlew installDevRelease` (with JAVA_HOME and ANDROID_HOME set as in Task 1)
    2. Present verification checklist to user
  </action>
  <what-built>
    Dev release build with distinct launcher icon (red "DEV" diagonal ribbon overlay) and "DEV" badge on onboarding screen welcome step. The devRelease APK has been built and can be installed.
  </what-built>
  <how-to-verify>
    1. Install the dev release on your phone:
       ```
       export JAVA_HOME="/opt/homebrew/Cellar/openjdk@21/21.0.10/libexec/openjdk.jdk/Contents/Home"
       export ANDROID_HOME="/opt/homebrew/share/android-commandlinetools"
       ./gradlew installDevRelease
       ```
    2. Check the launcher — the Karakept Dev app icon should show a red "DEV" ribbon in the top-right corner
    3. Open the app and go through onboarding (or clear app data first) — the welcome step bookmark icon should show a small red "DEV" badge at the bottom-right
    4. Verify the production app icon (if installed) does NOT have any DEV overlay
  </how-to-verify>
  <verify>User visually confirms launcher icon and onboarding badge</verify>
  <done>User approves visual appearance of DEV icon overlay and badge</done>
  <resume-signal>Type "approved" or describe any visual issues with the icon/badge</resume-signal>
</task>

</tasks>

<verification>
- All existing tests pass (testDebugUnitTest exits 0)
- devRelease APK assembles successfully
- Desktop target compiles (isDevBuild = false)
- Launcher icon visually shows DEV ribbon (human verified)
- OnboardingScreen DEV badge visible only in dev builds (human verified)
</verification>

<success_criteria>
- devRelease variant produces APK with distinct icon overlay
- Production builds are unaffected (no DEV ribbon, no DEV badge)
- Desktop build compiles with isDevBuild hardcoded to false
- All ~201 existing tests pass
- User confirms visual correctness on device
</success_criteria>

<output>
After completion, create `.planning/phases/22-dev-release-icon-and-label-differentiation/22-01-SUMMARY.md`
</output>
