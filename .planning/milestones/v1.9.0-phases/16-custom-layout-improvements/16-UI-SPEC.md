---
phase: 16
slug: custom-layout-improvements
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-30
---

# Phase 16 -- UI Design Contract

> Visual and interaction contract for the custom layout improvements phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Kotlin Multiplatform / Compose) |
| Preset | not applicable |
| Component library | Material Design 3 (`androidx.compose.material3`) |
| Icon library | `androidx.compose.material.icons` (filled + outlined) |
| Font | System default (Material 3 default typography) |

**Source:** CLAUDE.md project conventions, existing codebase.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (2.dp) | Tag chip vertical padding, compact metadata top padding |
| sm | 8px (8.dp) | Section title bottom padding, radio option start padding, field gaps |
| md | 12px (12.dp) | Compact layout horizontal padding, radio option horizontal/vertical padding, list row spacing |
| lg | 16px (16.dp) | Card content padding, toggle row horizontal padding, section padding |
| xl | 24px (24.dp) | Section breaks between editor setting groups |
| 2xl | 48px (48.dp) | Not used in this phase |
| 3xl | 80px (80.dp) | Bottom spacer in scrollable editor content |

**Named exceptions (justified):**

| Value | Rationale |
|-------|-----------|
| 12dp (md) | Matches established MD3 ListItem padding pattern used throughout the codebase. Also used for `Arrangement.spacedBy(12.dp)` in list row spacing. Not in the standard {4, 8, 16, 24, 32, 48, 64} set but is a multiple of 4 and consistent with existing code. |
| 80dp (3xl) | Bottom spacer to ensure scrollable editor content clears the FAB. Not in the standard set but is a multiple of 4 and matches the existing `LayoutEditorScreen` pattern. |

**Source:** Existing `LayoutEditorScreen.kt` and `BookmarkLayouts.kt` spacing values.

---

## Typography

All typography uses `MaterialTheme.typography.*` tokens. No hardcoded sizes.

| Role | Token | Usage in This Phase |
|------|-------|---------------------|
| Section heading | `titleLarge` | Editor section headers ("Show", "Layout", "URL Display", etc.) |
| Option title / Card title | `titleMedium` | Toggle row labels, slider labels, bookmark title in List/Card layouts |
| Option label / Content text | `bodyLarge` | Radio option primary text, dialog list item primary text, bookmark title in compact-style List layouts |
| Option description / Metadata | `bodySmall` | Radio option secondary text, URL display in bookmark cards/lists, date, reading time, URL/domain text in bookmark items |

**Note:** Line height: `MaterialTheme.typography` carries spec-compliant line heights -- no override needed.

**Source:** Existing `LayoutEditorScreen.kt` and `BookmarkLayouts.kt` typography tokens.

---

## Color

All colors use `MaterialTheme.colorScheme.*` tokens. No hardcoded hex values.

| Role | Token | Usage |
|------|-------|-------|
| Dominant surface | `surface` / `background` | Scaffold background, card container default |
| Card container | `CardDefaults.cardColors()` | All bookmark layout cards (list, card) |
| Secondary surface | `surfaceVariant` | Thumbnail placeholder background, progress track |
| Accent | `primary` | Selected radio icon tint, selected option icons, selection border, progress indicator, "Create new layout" button icon |
| Text primary | `onSurface` (implicit) | Title text, option labels |
| Text secondary | `onSurfaceVariant` | Description text, date, URL/domain display, option descriptions |
| Selection highlight | `primaryContainer` at 0.3 alpha | Selected bookmark card background |
| Active highlight | `secondaryContainer` at 0.3 alpha | Active bookmark card background (desktop) |

Accent reserved for: selected radio button icons in editor, "Create new layout" button icon/text, selection borders on bookmark cards, linear progress indicator.

**Note:** 60/30/10 not applicable -- surface-dominant settings screen; primary used only for accent per reserved-for list.

**Source:** Existing `BookmarkLayouts.kt` and `LayoutEditorScreen.kt` color usage.

---

## Focal Point

The primary visual anchor for `LayoutEditorScreen` is the `PreviewBookmarkItem` composable -- all toggle and radio changes update this live preview, making it the user's reference point for every edit.

---

## Component Inventory -- New UI Elements

### 1. URL/Domain Display in Bookmark Layouts

**Where:** `BookmarkCardLayout`, `BookmarkListLayout` (unified, replaces `BookmarkCompactListLayout`)

| Property | Specification |
|----------|---------------|
| Typography | `MaterialTheme.typography.bodySmall` |
| Color | `MaterialTheme.colorScheme.onSurfaceVariant` |
| Max lines | 1 |
| Overflow | `TextOverflow.Ellipsis` |
| Icon | `Icons.Default.Language` at 14.dp, tinted `onSurfaceVariant`, 4.dp end padding -- only when `urlPosition` is in a standalone row |
| Domain format | Parse hostname from URL, strip "www." prefix (e.g., "https://www.github.com/foo" becomes "github.com") |
| Full URL format | Display raw URL string, single line, ellipsis truncation |

**URL Position behavior by layout type:**

| Position Value | List Layout | Card Layout |
|----------------|-------------|-------------|
| `BELOW_TITLE` | Rendered as a `Text` directly below the title, above description (4.dp top padding) | Rendered below title in the content column (4.dp top padding) |
| `METADATA_ROW` | Rendered inline in the metadata row (date/reading time row), before the spacer | Rendered inline in the date/reading time row, before the spacer |

### 2. Layout Type Selector (Unified)

**Where:** `LayoutEditorContent`, "Layout" section

| Property | Specification |
|----------|---------------|
| Options | "Card" (icon: `Icons.Default.Window`) and "List" (icon: `Icons.AutoMirrored.Filled.ViewList`) |
| Removed | "Compact List" radio option -- no longer exists as a separate type |
| Pattern | Reuse existing `LayoutRadioOption` composable |
| Card description | "Large hero images with title below" (unchanged) |
| List description | "Row layout with configurable thumbnail, title, and metadata" |

### 3. New Toggle Rows in Editor "Show" Section

Added to the existing `Card` in the "Show" section of `LayoutEditorContent`:

| Toggle | Icon | Label | Default (List) | Default (Card) | Condition |
|--------|------|-------|-----------------|-----------------|-----------|
| Description | `Icons.Default.Notes` | "Description" | `true` | `true` | Always visible |
| URL / Domain | `Icons.Default.Link` | "URL" | `false` | `false` | Always visible |

When "URL" toggle is ON, show an additional inline radio group:

| Sub-option | Label | Description |
|------------|-------|-------------|
| `DOMAIN_ONLY` | "Domain only" | "e.g., github.com" |
| `FULL_URL` | "Full URL" | "e.g., https://github.com/repo/..." |

### 4. New Radio Section: "URL Position"

**Where:** `LayoutEditorContent`, new section below "Metadata Position" (only visible when `showUrl` is `true`)

| Property | Specification |
|----------|---------------|
| Section title | "URL Position" (typography: `titleLarge`, 8.dp bottom padding) |
| Container | `Card(modifier = Modifier.fillMaxWidth())` |
| Options | `LayoutRadioOption` composables with `HorizontalDivider` separators |
| Condition | Only visible when `showUrl == true` |

Options:

| Value | Title | Description | Icon |
|-------|-------|-------------|------|
| `BELOW_TITLE` | "Below title" | "URL appears between title and description" | `Icons.Default.Link` |
| `METADATA_ROW` | "In metadata row" | "URL appears alongside date and reading time" | `Icons.Default.Link` |

### 5. New Radio Section: "Description Position" (List layout only)

**Where:** `LayoutEditorContent`, new section after "Show" toggles (only visible when layout type is LIST AND `showDescription` is `true`)

| Property | Specification |
|----------|---------------|
| Section title | "Description Position" (typography: `titleLarge`, 8.dp bottom padding) |
| Visibility | `layout.layoutType == LayoutType.LIST.name && layout.showDescription` |

Options:

| Value | Title | Description |
|-------|-------|-------------|
| `BELOW_TITLE` | "Below title" | "Description appears under the title text" |
| `ABOVE_METADATA` | "Above metadata" | "Description appears between title area and metadata row" |

**Note:** Card layout does NOT get description position -- Card's vertical structure always places description below title per D-03.

### 6. "Create New Layout" Button in Per-List Picker Dialog

**Where:** `PerListSettingsScreen.kt`, inside the `AlertDialog` for layout selection

| Property | Specification |
|----------|---------------|
| Position | After the last layout option, separated by `HorizontalDivider` |
| Component | `TextButton` with icon |
| Icon | `Icons.Default.Add`, tinted `MaterialTheme.colorScheme.primary` |
| Label | "Create new layout" |
| Typography | `MaterialTheme.typography.bodyLarge` |
| Color | `MaterialTheme.colorScheme.primary` |
| Padding | `vertical = 8.dp` to match existing row rhythm |
| Behavior | Dismiss the picker dialog, then navigate to `LayoutEditorScreen(layoutId = null)`. On return, the picker re-opens with the new layout in the list. |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (editor) | "Save Layout" |
| Create layout button | "Create new layout" |
| List type description | "Row layout with configurable thumbnail, title, and metadata" |
| URL toggle label | "URL" |
| URL domain-only option | "Domain only" with description "e.g., github.com" |
| URL full option | "Full URL" with description "e.g., https://github.com/repo/..." |
| URL position section title | "URL Position" |
| URL below-title option | "Below title" with description "URL appears between title and description" |
| URL metadata-row option | "In metadata row" with description "URL appears alongside date and reading time" |
| Description toggle label | "Description" |
| Description position section title | "Description Position" |
| Description below-title option | "Below title" with description "Description appears under the title text" |
| Description above-metadata option | "Above metadata" with description "Description appears between title area and metadata row" |
| URL display mode section title | "URL Display" |
| Empty state (layouts list) | Not applicable -- built-in presets always present |
| Error state | Not applicable -- layout operations are local DataStore, no network errors |
| Destructive confirmation | Not applicable -- no destructive actions introduced in this phase |

---

## Interaction Contracts

### Live Preview Updates

All toggle and radio changes in the editor MUST update `PreviewBookmarkItem` immediately (same frame). The preview composable already receives the full `BookmarkLayout` state -- new fields (`showDescription`, `showUrl`, `urlDisplayMode`, `urlPosition`, `descriptionPosition`) must be wired through identically.

### Migration Behavior (COMPACT_LIST to LIST)

When loading existing layouts with `layoutType == "COMPACT_LIST"`:
- Auto-map to `LIST` type
- Set `showDescription = false`
- Set `showTags = false` (preserve existing value)
- Set `thumbnailSize = 48` (preserve existing value)
- Set `metadataPosition = "BESIDE"` (preserve existing value)

The `COMPACT_LIST` enum value remains in `LayoutType.fromString()` for deserialization but maps to `LIST`. Built-in Compact preset becomes a LIST variant with compact defaults.

### Per-List Picker Navigation Flow

1. User taps "Layout" in per-list settings
2. `AlertDialog` opens with layout options + "Create new layout" footer button
3. User taps "Create new layout"
4. Dialog dismisses
5. Navigator pushes `LayoutEditorScreen(layoutId = null)`
6. User configures and taps "Save Layout"
7. Navigator pops back to per-list settings
8. If user opens the layout picker again, the new layout appears in the list (via `allLayouts` StateFlow refresh)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| not applicable | none | Compose Multiplatform -- no shadcn registry |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
