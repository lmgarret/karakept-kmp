---
phase: 01-println-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/HighlightRepository.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt
  - composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollRestoration.kt
autonomous: true
requirements: [LOG-01]

must_haves:
  truths:
    - "BookmarkRepository.kt has zero println calls"
    - "HighlightRepository.kt has zero println calls"
    - "ImageCacheManager.kt has zero println calls and uses AppLogger instead"
    - "HtmlRenderer.android.kt has zero println calls"
    - "ViewerScrollRestoration.kt has zero println calls"
    - "Hot-path per-bookmark detail logging in BookmarkRepository is removed per D-04/D-05"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt"
      provides: "AppLogger calls replacing 17 printlns"
      contains: "AppLogger"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/HighlightRepository.kt"
      provides: "AppLogger calls replacing 13 printlns"
      contains: "AppLogger"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt"
      provides: "AppLogger calls replacing 5 printlns"
      contains: "AppLogger"
    - path: "composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt"
      provides: "AppLogger call replacing 1 println"
      contains: "AppLogger"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollRestoration.kt"
      provides: "AppLogger call replacing 1 println"
      contains: "AppLogger"
  key_links:
    - from: "BookmarkRepository.kt"
      to: "AppLogger"
      via: "import com.karakept.app.utils.AppLogger"
      pattern: "import com\\.karakept\\.app\\.utils\\.AppLogger"
    - from: "HighlightRepository.kt"
      to: "AppLogger"
      via: "import com.karakept.app.utils.AppLogger"
      pattern: "import com\\.karakept\\.app\\.utils\\.AppLogger"
---

<objective>
Replace all println calls in BookmarkRepository.kt (17), HighlightRepository.kt (13), ImageCacheManager.kt (5), HtmlRenderer.android.kt (1), and ViewerScrollRestoration.kt (1) with AppLogger calls or remove them.

Purpose: These 5 files account for the remaining ~37 println calls. Most are 1:1 conversions per D-08. BookmarkRepository has one hot-path line to remove per D-05.
Output: Five files with zero println calls, using structured AppLogger logging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-println-cleanup/01-CONTEXT.md

@composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt
</context>

<interfaces>
<!-- AppLogger API — the replacement target for all println calls -->
From composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt:
```kotlin
object AppLogger {
    fun d(tag: String, message: String)
    fun i(tag: String, message: String)
    fun w(tag: String, message: String, throwable: Throwable? = null)
    fun e(tag: String, message: String, throwable: Throwable? = null)
}
```

<!-- ImageCacheManager already has TAG pattern to follow -->
From composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt:
```kotlin
companion object {
    private const val TAG = "ImageCacheManager"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace println calls in BookmarkRepository.kt and HighlightRepository.kt</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/HighlightRepository.kt
  </files>
  <read_first>
    - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt
    - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/HighlightRepository.kt
    - composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt
  </read_first>
  <action>
**BookmarkRepository.kt** (17 println calls, tag `"BookmarkRepository"` per D-09):

Add `import com.karakept.app.utils.AppLogger` to imports.

Convert 1:1 per D-08 with these severity mappings:

Error/failure messages -> `AppLogger.e("BookmarkRepository", ...)` per D-01:
- "Polling fetch failed: ${e.message}" (line 96)
- "Failed to download content asset ${contentAsset.id}: ${e.message}" (line 220)
- "Failed to cache images in HTML: ${e.message}" (line 234)
- "Failed to download content asset ${contentAsset.id}: ${e.message}" (line 329)
- "CONTENT: ERROR fetching remote content - ${e.message}" (line 344)
- "Failed to cache banner image: ${e.message}" (line 420)
- "Failed to cache screenshot: ${e.message}" (line 450)
- "Background sync failed for new bookmark: ${e.message}" (line 138)

Debug messages -> `AppLogger.d("BookmarkRepository", ...)` per D-03:
- "Polling for bookmark parsing: attempt $attempts..." (line 99)
- "getBookmarksPaged: Using list-filtered query..." (line 261)
- "getBookmarksPaged: status=$status..." (line 277)
- "CONTENT: Downloading content from asset..." (line 324)
- "CONTENT: Downloaded ${content.length} chars..." (line 327)
- "CONTENT: Using note/text content..." (line 338)
- "Cached banner image for bookmark..." (line 417)
- "Cached screenshot for bookmark..." (line 447)

**Hot-path removal per D-04/D-05:**
- Line 279: `println("  - ${bookmark.originalRemoteId} (${bookmark.title}) listIds='${bookmark.listIds}' createdAt=${bookmark.createdAt}")` -- This is per-bookmark detail inside getBookmarksPaged results iteration. REMOVE this line entirely. Do not replace with AppLogger.

**HighlightRepository.kt** (13 println calls, tag `"HighlightRepository"` per D-09):

Add `import com.karakept.app.utils.AppLogger` to imports.

Convert 1:1 per D-08 with these severity mappings:

Error message -> `AppLogger.e("HighlightRepository", ...)` per D-01:
- "Error syncing highlights: ${e.message}" (line 69)

Debug messages -> `AppLogger.d("HighlightRepository", ...)` per D-03:
- "syncHighlights received ${remoteHighlights.size} highlights from server" (line 34)
- "Deleted all non-pending highlights for server (server returned empty)" (line 44)
- "Deleted local highlights not in remote list" (line 48)
- "Inserted ${entities.size} highlights into local DB" (line 67)
- "Fetching highlights for bookmark $bookmarkRemoteId" (line 75)
- "Received ${remoteHighlights.size} highlights from server" (line 77)
- "Deleted all non-pending highlights for bookmark (server returned empty)" (line 87)
- "Deleted local highlights not in remote list: $remoteIds" (line 91)
- "Inserted ${entities.size} highlights into local DB" (line 110)
- "updateHighlight called - highlightRemoteId=$highlightRemoteId, note=$note, color=$color" (line 156)
- "updateHighlight - existing highlight found: ${existing != null}" (line 161)
- "updateHighlight - local DB updated" (line 167)
  </action>
  <verify>
    <automated>cd /var/home/lm/git/karakept-kmp && PASS=true; for f in composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/HighlightRepository.kt; do COUNT=$(grep -c "println" "$f"); BASENAME=$(basename "$f"); if [ "$COUNT" -eq 0 ]; then echo "PASS: $BASENAME"; else echo "FAIL: $BASENAME ($COUNT println calls remain)"; PASS=false; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "println" BookmarkRepository.kt` returns `0`
    - `grep -c "println" HighlightRepository.kt` returns `0`
    - `grep -c "AppLogger" BookmarkRepository.kt` returns a value greater than 0
    - `grep -c "AppLogger" HighlightRepository.kt` returns a value greater than 0
    - `grep "import com.karakept.app.utils.AppLogger" BookmarkRepository.kt` matches
    - `grep "import com.karakept.app.utils.AppLogger" HighlightRepository.kt` matches
    - `grep 'AppLogger.e(' BookmarkRepository.kt` matches (failure messages use error level)
    - `grep 'AppLogger.e(' HighlightRepository.kt` matches (sync error uses error level)
    - BookmarkRepository.kt does NOT contain the per-bookmark detail line `originalRemoteId.*title.*listIds.*createdAt` (hot-path removed per D-05)
  </acceptance_criteria>
  <done>BookmarkRepository.kt has zero println calls with 16 converted to AppLogger and 1 hot-path line removed. HighlightRepository.kt has zero println calls with all 13 converted to AppLogger. Correct severity levels applied throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Replace println calls in ImageCacheManager.kt, HtmlRenderer.android.kt, and ViewerScrollRestoration.kt</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt
    composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollRestoration.kt
  </files>
  <read_first>
    - composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt
    - composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt
    - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollRestoration.kt
    - composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt
  </read_first>
  <action>
**ImageCacheManager.kt** (5 println calls):

This file already has `companion object { private const val TAG = "ImageCacheManager" }` and uses `$TAG:` prefix in its println messages. Add `import com.karakept.app.utils.AppLogger` and replace each println with AppLogger using the existing TAG constant.

Important: Remove the `$TAG: ` prefix from the message strings since AppLogger adds the tag automatically in its output format `"{level}/$tag: $message"`.

Error/failure messages -> `AppLogger.e(TAG, ...)` per D-01:
- `println("$TAG: Failed to cache image '$src': ${e.message}")` (line 86) -> `AppLogger.e(TAG, "Failed to cache image '$src': ${e.message}")`
- `println("$TAG: Failed to process HTML for image caching: ${e.message}")` (line 95) -> `AppLogger.e(TAG, "Failed to process HTML for image caching: ${e.message}")`
- `println("$TAG: Failed to save image to disk: ${e.message}")` (line 141) -> `AppLogger.e(TAG, "Failed to save image to disk: ${e.message}")`

Warning messages -> `AppLogger.w(TAG, ...)` per D-02:
- `println("$TAG: HTTP ${response.status} for image: $url")` (line 126) -> `AppLogger.w(TAG, "HTTP ${response.status} for image: $url")`
- `println("$TAG: Skipping oversized image (${bytes.size} bytes): $url")` (line 134) -> `AppLogger.w(TAG, "Skipping oversized image (${bytes.size} bytes): $url")`

**HtmlRenderer.android.kt** (1 println call, tag `"HtmlRenderer"` per D-09):

Add `import com.karakept.app.utils.AppLogger` to imports.

- `println("WebView Log: $message")` (line 673) -> `AppLogger.d("HtmlRenderer", "WebView Log: $message")`

**ViewerScrollRestoration.kt** (1 println call, tag `"ViewerScrollRestoration"` per D-09):

Add `import com.karakept.app.utils.AppLogger` to imports.

- `println("ScrollGuard: Unintended jump to ${scrollState.firstVisibleItemIndex}:${scrollState.firstVisibleItemScrollOffset}. " + ...)` (line 63) -> `AppLogger.w("ViewerScrollRestoration", "Unintended jump to ${scrollState.firstVisibleItemIndex}:${scrollState.firstVisibleItemScrollOffset}. " + ...)` -- This is a warning about unexpected scroll behavior per D-02.
  </action>
  <verify>
    <automated>cd /var/home/lm/git/karakept-kmp && PASS=true; for f in composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollRestoration.kt; do COUNT=$(grep -c "println" "$f"); BASENAME=$(basename "$f"); if [ "$COUNT" -eq 0 ]; then echo "PASS: $BASENAME"; else echo "FAIL: $BASENAME ($COUNT println calls remain)"; PASS=false; fi; done; $PASS && echo "ALL PASS" || echo "SOME FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "println" ImageCacheManager.kt` returns `0`
    - `grep -c "println" HtmlRenderer.android.kt` returns `0`
    - `grep -c "println" ViewerScrollRestoration.kt` returns `0`
    - `grep 'AppLogger.e(TAG' ImageCacheManager.kt` matches (failure messages use error level with TAG constant)
    - `grep 'AppLogger.w(TAG' ImageCacheManager.kt` matches (warning messages use warning level with TAG constant)
    - `grep "import com.karakept.app.utils.AppLogger" HtmlRenderer.android.kt` matches
    - `grep "import com.karakept.app.utils.AppLogger" ViewerScrollRestoration.kt` matches
    - `grep 'AppLogger.d("HtmlRenderer"' HtmlRenderer.android.kt` matches
    - `grep 'AppLogger.w("ViewerScrollRestoration"' ViewerScrollRestoration.kt` matches
  </acceptance_criteria>
  <done>ImageCacheManager.kt has zero println calls with all 5 converted using existing TAG constant. HtmlRenderer.android.kt and ViewerScrollRestoration.kt each have zero println calls with their single println converted to AppLogger. Correct severity levels applied: .e() for failures, .w() for warnings/unexpected states, .d() for debug traces.</done>
</task>

</tasks>

<verification>
All 5 files in this plan have zero println calls remaining. AppLogger import is present in all files. Severity levels follow D-01 through D-03 mapping. Hot-path per-bookmark detail line in BookmarkRepository removed per D-04/D-05.

Full phase verification (run after both plans complete):
`grep -rc "println" composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/ composeApp/src/commonMain/kotlin/com/karakept/app/utils/ImageCacheManager.kt composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollRestoration.kt` -- only AppLogger.kt should show println counts (its internal implementation).
</verification>

<success_criteria>
- All 5 files show 0 println calls via grep
- All 5 files show non-zero AppLogger calls via grep
- No hot-path per-bookmark logging remains in BookmarkRepository.kt
</success_criteria>

<output>
After completion, create `.planning/phases/01-println-cleanup/01-02-SUMMARY.md`
</output>
