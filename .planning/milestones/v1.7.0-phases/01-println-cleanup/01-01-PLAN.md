---
phase: 01-println-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt
autonomous: true
requirements: [LOG-01]

must_haves:
  truths:
    - "BookmarkActionsRepositorySync.kt has zero println calls"
    - "BookmarkActionsRepository.kt has zero println calls"
    - "Sync action logging is consolidated: one .d() at action start, one at success, .e() on failure per D-07"
    - "Hot-path per-action iteration logging is removed per D-04"
    - "ReadProgressSync-prefixed messages use tag ReadProgressSync per D-10"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt"
      provides: "Consolidated AppLogger calls replacing 53 printlns"
      contains: "AppLogger"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt"
      provides: "AppLogger calls replacing 7 printlns"
      contains: "AppLogger"
  key_links:
    - from: "BookmarkActionsRepositorySync.kt"
      to: "AppLogger"
      via: "import com.karakept.app.utils.AppLogger"
      pattern: "import com\\.karakept\\.app\\.utils\\.AppLogger"
    - from: "BookmarkActionsRepository.kt"
      to: "AppLogger"
      via: "import com.karakept.app.utils.AppLogger"
      pattern: "import com\\.karakept\\.app\\.utils\\.AppLogger"
---

<objective>
Replace all println calls in BookmarkActionsRepositorySync.kt (53 calls) and BookmarkActionsRepository.kt (7 calls) with AppLogger calls or remove them.

Purpose: These two files account for ~60 of the ~97 total println calls. BookmarkActionsRepositorySync requires consolidation (D-07) rather than 1:1 conversion, making it the most complex file in this phase.
Output: Two files with zero println calls, using structured AppLogger logging with appropriate severity levels.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-println-cleanup/01-CONTEXT.md

@composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt
</context>

<interfaces>
<!-- AppLogger API — the replacement target for all println calls -->
From composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt:
```kotlin
object AppLogger {
    fun d(tag: String, message: String)
    fun i(tag: String, message: String)
    fun w(tag: String, message: String, throwable: Throwable? = null)
    fun e(tag: String, message: String, throwable: Throwable? = null)
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace println calls in BookmarkActionsRepositorySync.kt with consolidated AppLogger calls</name>
  <files>composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt</files>
  <read_first>
    - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt
    - composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt
  </read_first>
  <action>
Add `import com.karakept.app.utils.AppLogger` to the file imports.

This file has 53 println calls that need consolidation per D-07, not 1:1 conversion. Apply these rules:

**Tag assignment (per D-09, D-10):**
- Lines prefixed with `"ReadProgressSync:"` (lines 32, 36, 41, 45, 48, 52, 62, 65, 232, 234) use tag `"ReadProgressSync"`
- All other lines use tag `"BookmarkActionsRepositorySync"`

**Consolidation of executeAction method (per D-07):**
The `executeAction` function (starting ~line 121) has verbose per-action-type step logging. Consolidate to:
- ONE `AppLogger.d(TAG, "executeAction START ${action.actionType} bookmark=${action.bookmarkRemoteId}")` at entry (keep line 121's content)
- REMOVE all intermediate step traces — these are the lines like:
  - "Calling updateBookmark with archived=true" (line 167)
  - "Calling updateBookmark with archived=false" (line 171)
  - "Calling updateBookmark with favourited=true" (line 175)
  - "Calling updateBookmark with favourited=false" (line 179)
  - "Calling deleteBookmark" (line 183)
  - "UPDATE_TAGS - Replacing tags..." (line 189)
  - "Current tags:" (line 196)
  - "Tags to add:" / "Tags to remove:" (lines 202-203)
  - "Tags to remove mapped to IDs:" (line 208)
  - "Detaching..." / "Detach response:" (lines 210, 212)
  - "Warning: Could not find tag IDs..." (line 214) -- KEEP this one as `AppLogger.w()` since it indicates a problem
  - "Attaching..." / "Attach response:" (lines 220, 222)
  - "Skipping legacy..." (line 227)
  - "Calling addBookmarkToList" (line 239)
  - "Calling removeBookmarkFromList" (line 245)
  - "Calling createHighlight..." (line 257)
  - "Highlight created successfully..." (line 261)
  - "Replacing tempId=..." (line 266)
  - "Successfully updated highlight..." (line 281)
  - "DELETE_HIGHLIGHT actionData=..." (line 291)
  - "DELETE_HIGHLIGHT parsed data=..." (line 293)
  - "Calling deleteHighlight..." (line 299)
  - "Calling updateHighlight..." (line 328)
- ONE `AppLogger.d(TAG, "executeAction SUCCESS ${action.actionType} bookmark=${action.bookmarkRemoteId}")` at success (replace lines 333+336)
- Convert error/failure messages to `AppLogger.e()`:
  - "Server not found for action" (line 127) -> `AppLogger.e(TAG, ...)`
  - "action missing originalRemoteId" (line 141) -> `AppLogger.e(TAG, ...)`
  - "Bookmark not found locally" (line 155) -> `AppLogger.w(TAG, ...)`
  - "ERROR - highlightId is null" (line 296) -> `AppLogger.e(TAG, ...)`
  - "Could not find highlight with tempId" (line 283) -> `AppLogger.w(TAG, ...)`
  - "Error updating highlight with server ID" (line 286) -> `AppLogger.e(TAG, ...)`
  - "Highlight not found on server, treating as already deleted" (line 316) -> `AppLogger.w(TAG, ...)`
  - "Skipping delete for temp highlight ID" (line 303) -> `AppLogger.d(TAG, ...)`

**Per-item iteration logging (per D-04, D-05):**
- "Processing action ${action.actionType} for bookmark..." (line 99) — this is called PER action in a loop. REMOVE entirely.

**Entry/exit sync logging (per D-06) — KEEP as .d():**
- "Found ${actions.size} pending actions" (line 96) -> `AppLogger.d(TAG, ...)`
- "Finished processing all pending actions" (line 104) -> `AppLogger.d(TAG, ...)`
- "Executing ${action.actionType} on server" (line 163) -> `AppLogger.d(TAG, ...)`

**ReadProgressSync lines — convert 1:1 (per D-08) EXCEPT hot-path:**
- Lines 32, 36, 41, 45, 48, 52, 62, 65 are in pullReadingProgressFromServer (called once per sync) -> convert to appropriate severity:
  - "pull ABORT" messages (lines 36, 41) -> `AppLogger.w("ReadProgressSync", ...)`
  - "pull -- server returned null" (line 48) -> `AppLogger.w("ReadProgressSync", ...)`
  - All others -> `AppLogger.d("ReadProgressSync", ...)`
- Lines 232, 234 are in executeAction (called once per action) -> `AppLogger.d("ReadProgressSync", ...)`

**Failure line (per D-01):**
- "Failed to pull reading progress" (line 69) -> `AppLogger.e("BookmarkActionsRepositorySync", ...)`
- "Action failed after 5 retries" (line 351) -> `AppLogger.e(TAG, ...)`
  </action>
  <verify>
    <automated>cd /var/home/lm/git/karakept-kmp && grep -c "println" composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt | grep -q "^0$" && echo "PASS: zero println" || echo "FAIL: println calls remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "println" BookmarkActionsRepositorySync.kt` returns `0`
    - `grep -c "AppLogger" BookmarkActionsRepositorySync.kt` returns a value greater than 0
    - `grep "import com.karakept.app.utils.AppLogger" BookmarkActionsRepositorySync.kt` matches
    - `grep 'AppLogger.e(' BookmarkActionsRepositorySync.kt` matches (error-level calls exist)
    - `grep 'AppLogger.w(' BookmarkActionsRepositorySync.kt` matches (warning-level calls exist)
    - `grep 'AppLogger.d(' BookmarkActionsRepositorySync.kt` matches (debug-level calls exist)
    - `grep '"ReadProgressSync"' BookmarkActionsRepositorySync.kt` matches (ReadProgressSync tag used per D-10)
    - No lines contain `"Calling updateBookmark with"` (consolidated away per D-07)
    - No lines contain `"Calling deleteBookmark"` (consolidated away per D-07)
    - No lines contain `"Processing action .* for bookmark"` (hot-path removed per D-04)
  </acceptance_criteria>
  <done>BookmarkActionsRepositorySync.kt has zero println calls. Logging is consolidated per D-07: one .d() at action start, one at success, .e()/.w() on failures. Hot-path per-action logging removed per D-04. ReadProgressSync tag used per D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Replace println calls in BookmarkActionsRepository.kt with AppLogger calls</name>
  <files>composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt</files>
  <read_first>
    - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt
    - composeApp/src/commonMain/kotlin/com/karakept/app/utils/AppLogger.kt
  </read_first>
  <action>
Add `import com.karakept.app.utils.AppLogger` to the file imports.

This file has 7 println calls. Convert 1:1 per D-08 with these severity mappings:

**Tag assignment (per D-09, D-10):**
- Lines prefixed with `"ReadProgressSync:"` (lines 202, 213) use tag `"ReadProgressSync"`
- All other lines use tag `"BookmarkActionsRepository"`

**Severity mapping:**
- `println("ReadProgressSync: queueReadingProgressUpdate remoteId=$bookmarkRemoteId serverId=$serverId percent=$progressPercent")` (line 202) -> `AppLogger.d("ReadProgressSync", "queueReadingProgressUpdate remoteId=$bookmarkRemoteId serverId=$serverId percent=$progressPercent")`
- `println("ReadProgressSync: queued pending action successfully")` (line 213) -> `AppLogger.d("ReadProgressSync", "queued pending action successfully")`
- `println("BookmarkActionsRepository: queueUpdateHighlight called - highlightRemoteId=$highlightRemoteId, note=$note, color=$color")` (line 411) -> `AppLogger.d("BookmarkActionsRepository", "queueUpdateHighlight called - highlightRemoteId=$highlightRemoteId, note=$note, color=$color")`
- `println("BookmarkActionsRepository: queueUpdateHighlight - actionData=$actionData")` (line 418) -> `AppLogger.d("BookmarkActionsRepository", "queueUpdateHighlight - actionData=$actionData")`
- `println("BookmarkActionsRepository: queueUpdateHighlight - action queued, triggering sync")` (line 425) -> `AppLogger.d("BookmarkActionsRepository", "queueUpdateHighlight - action queued, triggering sync")`
- `println("BookmarkActionsRepository: Triggering push-only sync for server ${server.id}")` (line 476) -> `AppLogger.d("BookmarkActionsRepository", "Triggering push-only sync for server ${server.id}")`
- `println("BookmarkActionsRepository: Error in auto-sync: ${e.message}")` (line 481) -> `AppLogger.e("BookmarkActionsRepository", "Error in auto-sync: ${e.message}")`
  </action>
  <verify>
    <automated>cd /var/home/lm/git/karakept-kmp && grep -c "println" composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt | grep -q "^0$" && echo "PASS: zero println" || echo "FAIL: println calls remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "println" BookmarkActionsRepository.kt` returns `0`
    - `grep -c "AppLogger" BookmarkActionsRepository.kt` returns a value greater than 0
    - `grep "import com.karakept.app.utils.AppLogger" BookmarkActionsRepository.kt` matches
    - `grep 'AppLogger.e(' BookmarkActionsRepository.kt` matches (at least the auto-sync error)
    - `grep '"ReadProgressSync"' BookmarkActionsRepository.kt` matches (ReadProgressSync tag used per D-10)
    - `grep '"BookmarkActionsRepository"' BookmarkActionsRepository.kt` matches (class name tag per D-09)
  </acceptance_criteria>
  <done>BookmarkActionsRepository.kt has zero println calls. All 7 former printlns converted to AppLogger with correct severity: .e() for the error case, .d() for all debug traces. ReadProgressSync tag used for reading-progress lines per D-10.</done>
</task>

</tasks>

<verification>
Both files in this plan have zero println calls remaining. AppLogger import is present in both. Consolidated logging in BookmarkActionsRepositorySync follows the pattern: one start log, one success log, error/warning logs on failure paths. No hot-path per-item logging remains.
</verification>

<success_criteria>
- `grep -rc "println" composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt` shows 0 for both files
- `grep -rc "AppLogger" composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt` shows non-zero for both files
</success_criteria>

<output>
After completion, create `.planning/phases/01-println-cleanup/01-01-SUMMARY.md`
</output>
