---
phase: 04-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/desktopTest/kotlin/com/karakept/app/data/repository/PendingActionQueueTest.kt
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Action queue processes actions in createdAt order"
    - "Conflicting actions (archive then unarchive) both execute, last wins"
    - "Actions with retryCount >= 5 are deleted"
    - "Server rejection increments retryCount and keeps action"
  artifacts:
    - path: "composeApp/src/desktopTest/kotlin/com/karakept/app/data/repository/PendingActionQueueTest.kt"
      provides: "Offline-first action queue unit tests"
      min_lines: 80
  key_links:
    - from: "PendingActionQueueTest.kt"
      to: "BookmarkActionsRepositorySync.kt"
      via: "Tests processPendingActions extension function"
      pattern: "processPendingActions"
---

<objective>
Add unit tests for the offline-first action queue covering ordering, conflict resolution, timeout handling, and server rejection scenarios.

Purpose: TEST-01 — the action queue is the highest-risk untested path; silent ordering bugs or lost actions corrupt user data.
Output: PendingActionQueueTest.kt with 4+ test cases covering all required scenarios.
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-test-coverage/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PendingActionQueueTest with all action queue scenarios</name>
  <files>composeApp/src/desktopTest/kotlin/com/karakept/app/data/repository/PendingActionQueueTest.kt</files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositorySync.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/data/local/entity/PendingActionEntity.kt
    composeApp/src/commonMain/kotlin/com/karakept/app/data/local/dao/PendingActionDao.kt
    composeApp/src/desktopTest/kotlin/com/karakept/app/data/integration/BookmarkSyncIntegrationTest.kt
  </read_first>
  <behavior>
    - Test 1 (ordering): Given actions with createdAt 1000, 2000, 3000 — processPendingActions calls remoteDataSource in that order (coVerifyOrder)
    - Test 2 (conflict): Given ARCHIVE action (createdAt=1) then UNARCHIVE action (createdAt=2) for same bookmarkId — both execute, second call has archived=false
    - Test 3 (timeout deletion): Given action with retryCount=5 — action is deleted from DAO without calling remoteDataSource
    - Test 4 (server rejection): Given remoteDataSource throws exception — retryCount is incremented via DAO update, action is NOT deleted
  </behavior>
  <action>
Create `PendingActionQueueTest.kt` in `composeApp/src/desktopTest/kotlin/com/karakept/app/data/repository/`.

Test setup:
- Construct a real `BookmarkActionsRepository` with MockK mocks for: `bookmarkDao`, `pendingActionDao`, `remoteDataSource`, `serverRepository`, `highlightDao` (nullable, pass null)
- Use `kotlinx.serialization.json.Json` for jsonSerializer
- Use a real `Mutex()` for actionMutex
- Check constructor visibility — if not directly accessible, use the same package (`com.karakept.app.data.repository`) so internal visibility works
- For dispatcher issues with `withContext(Dispatchers.IO)`: since DAOs are mocked (no real I/O), the IO dispatcher will return immediately. If tests hang, switch to `UnconfinedTestDispatcher`.

Create a helper function `pendingAction(actionType, bookmarkId, createdAt, retryCount)` that returns a `PendingActionEntity` with sensible defaults.

Mock `serverRepository` to return a test `Server` object (needed by processPendingActions).

**Test 1 — actionsProcessedInCreationOrder:**
- Mock `pendingActionDao.getPendingActionsList(serverId)` to return 3 actions with createdAt 1000, 2000, 3000 (different action types: ARCHIVE, FAVOURITE, ADD_TAG)
- Mock `remoteDataSource.updateBookmark(...)` to return success
- Call `repository.processPendingActions(server)`
- `coVerifyOrder` that remoteDataSource calls happen in createdAt order

**Test 2 — conflictingActionsLastWins:**
- Two actions for same bookmarkId: ARCHIVE (createdAt=1), then UNARCHIVE or re-archive=false (createdAt=2)
- Both should execute — verify both calls made, second has archived=false

**Test 3 — timeoutActionDeletedAfterMaxRetries:**
- Action with retryCount >= 5 (check exact threshold in BookmarkActionsRepositorySync.kt)
- Verify `pendingActionDao.deletePendingAction(action)` called
- Verify `remoteDataSource.updateBookmark` NOT called for this action

**Test 4 — serverRejectionIncrementsRetry:**
- Mock `remoteDataSource.updateBookmark(...)` to throw Exception
- Verify `pendingActionDao.updatePendingAction(match { it.retryCount == originalRetryCount + 1 })` called
- Verify action is NOT deleted
  </action>
  <verify>
    <automated>Tell user to run: ./gradlew :composeApp:desktopTest --tests "*.PendingActionQueueTest"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at composeApp/src/desktopTest/kotlin/com/karakept/app/data/repository/PendingActionQueueTest.kt
    - Contains @Test annotations for at least 4 test methods
    - Contains "coVerifyOrder" or "coVerify" for ordering verification
    - Contains "retryCount" for retry threshold testing
    - Contains "deletePendingAction" for timeout deletion verification
    - All tests use runTest from kotlinx-coroutines-test
  </acceptance_criteria>
  <done>4 test cases pass covering action ordering, conflict resolution, timeout deletion, and server rejection retry increment</done>
</task>

</tasks>

<verification>
All tests in PendingActionQueueTest pass: `./gradlew :composeApp:desktopTest --tests "*.PendingActionQueueTest"`
</verification>

<success_criteria>
- PendingActionQueueTest.kt exists with 4+ passing tests
- Tests cover: ordering by createdAt, conflict resolution, retry threshold deletion, server rejection retry increment
- Tests use MockK mocks (no Docker/integration test infrastructure needed)
- Tests run in seconds, not minutes
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-coverage/04-01-SUMMARY.md`
</output>
