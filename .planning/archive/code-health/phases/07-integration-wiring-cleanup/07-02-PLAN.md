---
phase: 07-integration-wiring-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/App.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreenModel.kt
autonomous: true
requirements: [SEC-02, ERR-01]

must_haves:
  truths:
    - "App startup triggers credential migration from DB to SecureCredentialStore"
    - "No println calls remain in BookmarkViewerScreenModel.kt"
    - "No println calls remain in App.kt"
    - "Non-hot-path debug info uses AppLogger.d instead of println"
    - "Hot-path code has zero logging (no println, no AppLogger)"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/App.kt"
      provides: "triggerMigration() call site and println-free code"
      contains: "triggerMigration"
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreenModel.kt"
      provides: "println-free screen model with AppLogger for non-hot-path diagnostics"
  key_links:
    - from: "App.kt"
      to: "ServerRepository.triggerMigration()"
      via: "LaunchedEffect(Unit) coroutine"
      pattern: "serverRepository\\.triggerMigration"
    - from: "BookmarkViewerScreenModel.kt"
      to: "AppLogger"
      via: "AppLogger.d() calls for non-hot-path logging"
      pattern: "AppLogger\\.d"
---

<objective>
Wire ServerRepository.triggerMigration() into App.kt startup and replace all println calls in App.kt and BookmarkViewerScreenModel.kt with AppLogger or removal.

Purpose: Close SEC-02 (credential migration never triggers) and ERR-01 (30 println calls remain in two files).
Output: Two modified files with migration wiring and structured logging.
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-wiring-cleanup/07-RESEARCH.md

<interfaces>
From data/repository/ServerRepository.kt (line 55):
```kotlin
suspend fun triggerMigration() {
    ensureMigrated()  // Mutex-protected, idempotent, no-op if already migrated
}
```

From utils/AppLogger.kt:
```kotlin
object AppLogger {
    fun d(tag: String, message: String)  // Debug
    fun w(tag: String, message: String)  // Warning
    fun e(tag: String, message: String, throwable: Throwable? = null)  // Error
}
```

Phase 6 decision: "Remove per-item debug println entirely rather than downgrade to AppLogger (hot-path should have zero logging)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire triggerMigration into App.kt startup and remove all println calls from App.kt</name>
  <files>composeApp/src/commonMain/kotlin/App.kt</files>
  <read_first>
    composeApp/src/commonMain/kotlin/App.kt
  </read_first>
  <action>
    **Add triggerMigration call:**
    After the existing backup auto-export LaunchedEffect block (lines 84-92), add a new LaunchedEffect for credential migration:
    ```kotlin
    // Migrate credentials from DB to SecureCredentialStore (best-effort, idempotent)
    LaunchedEffect(Unit) {
        try {
            serverRepository.triggerMigration()
        } catch (_: CancellationException) {
            throw it
        } catch (_: Exception) {
            // Migration is best-effort — existing creds work via DB fallback
        }
    }
    ```
    Note: `serverRepository` is already injected at line 30 (`val serverRepository = ...koinInject<...ServerRepository>()`). The `CancellationException` import already exists at line 22.

    **IMPORTANT:** The catch block must re-throw CancellationException. Use this pattern:
    ```kotlin
    } catch (e: CancellationException) {
        throw e
    } catch (_: Exception) {
    ```

    **Remove/replace println calls (10 total):**

    Lines 41, 48, 51 — Coil interceptor (HOT PATH — remove entirely):
    - Line 41: `println("🖼️ Coil loading image: $url")` — DELETE this line
    - Line 48: `println("🔐 Adding auth header for asset URL")` — DELETE this line
    - Line 51: `println("⚠️ No server found for authentication")` — DELETE this line

    Lines 103, 105, 112, 116, 119, 125, 128 — Navigation LaunchedEffect (NON-HOT PATH — replace with AppLogger):
    - Line 103: `println("📱 App LaunchedEffect...")` — Replace with `AppLogger.d("App", "LaunchedEffect. sharedUrl=$sharedUrl, openBookmarkId=$openBookmarkId")`
    - Line 105: `println("   Showing ShareBookmarkScreen")` — Replace with `AppLogger.d("App", "Showing ShareBookmarkScreen")`
    - Line 112: `println("   Parsing bookmark ID...")` — Replace with `AppLogger.d("App", "Parsing bookmark ID $bookmarkIdLong. Adding BookmarkViewerScreen.")`
    - Line 116: `println("   Error parsing openBookmarkId...")` — Replace with `AppLogger.w("App", "Error parsing openBookmarkId: ${e.message}")`
    - Line 119: `println("   No bookmark ID...")` — Replace with `AppLogger.d("App", "No bookmark ID, showing only MainScreen")`
    - Line 125: `println("   First launch...")` — Replace with `AppLogger.d("App", "First launch, showing OnboardingScreen")`
    - Line 128: `println("   No servers...")` — Replace with `AppLogger.d("App", "No servers, showing LoginScreen")`

    **Add import** (if not present): `import com.karakept.app.utils.AppLogger`
  </action>
  <verify>
    <automated>grep -c "println" composeApp/src/commonMain/kotlin/App.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "println" App.kt` returns 0
    - `grep -c "triggerMigration" App.kt` returns at least 1
    - App.kt contains `import com.karakept.app.utils.AppLogger`
    - App.kt contains `AppLogger.d("App",` for navigation logging (7 occurrences)
    - App.kt contains `AppLogger.w("App",` for error parsing (1 occurrence)
    - App.kt contains `serverRepository.triggerMigration()` inside a LaunchedEffect
    - CancellationException is re-thrown in the migration catch block
    - Coil interceptor lines that had println are now gone (no replacement — hot path)
  </acceptance_criteria>
  <done>triggerMigration() fires on app startup; all 10 println calls in App.kt are removed or replaced with AppLogger</done>
</task>

<task type="auto">
  <name>Task 2: Remove all println calls from BookmarkViewerScreenModel.kt</name>
  <files>composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreenModel.kt</files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreenModel.kt
  </read_first>
  <action>
    Replace or remove all 20 println calls per the Phase 6 decision (hot-path = remove, non-hot-path = AppLogger.d).

    **HOT PATH — Remove entirely (ReadProgressSync tracing, 8 lines):**
    - Line 170: `println("ReadProgressSync: debounce fired...")` — DELETE
    - Line 183: `println("ReadProgressSync: queueing server push...")` — DELETE
    - Line 190: `println("ReadProgressSync: skipping queue — no FullyLoaded...")` — DELETE
    - Line 193: `println("ReadProgressSync: skipping queue — trackReadingProgress...")` — DELETE
    - Line 206: `println("ReadProgressSync: onReadingStateChanged...")` — DELETE
    - Line 338: `println("ReadProgressSync: pulling from server...")` — DELETE
    - Line 343: `println("ReadProgressSync: pull result=...")` — DELETE
    - Line 353: `println("ReadProgressSync: skipping pull — offline...")` — DELETE

    **NON-HOT PATH — Replace with AppLogger.d (refresh flow, 8 lines):**
    - Line 254: `println("BookmarkViewerScreenModel: refreshBookmark called but state is not FullyLoaded")` — Replace with `AppLogger.d("ViewerModel", "refreshBookmark called but state is not FullyLoaded")`
    - Line 260: `println("BookmarkViewerScreenModel: refreshBookmark skipped - offline mode")` — Replace with `AppLogger.d("ViewerModel", "refreshBookmark skipped - offline mode")`
    - Line 264: `println("BookmarkViewerScreenModel: Starting refresh...")` — Replace with `AppLogger.d("ViewerModel", "Starting refresh for bookmark ${currentState.bookmark.remoteId}")`
    - Line 272: `println("BookmarkViewerScreenModel: Bookmark content synced")` — Replace with `AppLogger.d("ViewerModel", "Bookmark content synced")`
    - Line 279: `println("BookmarkViewerScreenModel: Syncing highlights...")` — Replace with `AppLogger.d("ViewerModel", "Syncing highlights for remoteId=$remoteId")`
    - Line 281: `println("BookmarkViewerScreenModel: Highlights synced")` — Replace with `AppLogger.d("ViewerModel", "Highlights synced")`
    - Line 290: `println("BookmarkViewerScreenModel: Server not found for serverId=...")` — Replace with `AppLogger.w("ViewerModel", "Server not found for serverId=${currentState.bookmark.serverId}")`
    - Line 299: `println("BookmarkViewerScreenModel: Refresh complete")` — Replace with `AppLogger.d("ViewerModel", "Refresh complete")`

    **NON-HOT PATH — Replace with AppLogger.d (highlight creation, 4 lines):**
    - Line 562: `println("BookmarkViewerScreenModel: createHighlight requested...")` — Replace with `AppLogger.d("ViewerModel", "createHighlight requested - text='${text.take(30)}...', start=$startOffset, end=$endOffset")`
    - Line 567: `println("BookmarkViewerScreenModel: Server not found...")` — Replace with `AppLogger.w("ViewerModel", "Server not found for serverId=${bookmark.serverId}")`
    - Line 571: `println("BookmarkViewerScreenModel: Calling highlightRepository...")` — Replace with `AppLogger.d("ViewerModel", "Calling highlightRepository.createHighlight for remoteId=$remoteId")`
    - Line 573: `println("BookmarkViewerScreenModel: Highlight created successfully...")` — Replace with `AppLogger.d("ViewerModel", "Highlight created successfully, id=$highlightId")`

    **Verify AppLogger import exists:** The file already imports `com.karakept.app.utils.AppLogger` (check and add if missing).

    **IMPORTANT:** Do NOT touch AppLogger calls that already exist in catch blocks (lines 293, 327, 407, 488, 576 — these are already AppLogger.e/w calls and must remain).
  </action>
  <verify>
    <automated>grep -c "println" composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreenModel.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "println" BookmarkViewerScreenModel.kt` returns 0
    - `grep -c "AppLogger.d" BookmarkViewerScreenModel.kt` returns at least 10 (existing + new)
    - `grep -c "AppLogger.w" BookmarkViewerScreenModel.kt` returns at least 2 (for "Server not found" warnings)
    - File contains `import com.karakept.app.utils.AppLogger`
    - Existing AppLogger.e calls in catch blocks (e.g., lines 293, 327) are unchanged
    - No ReadProgressSync println remains (all 8 deleted, not converted)
    - Refresh flow println converted to AppLogger.d with tag "ViewerModel"
    - Highlight println converted to AppLogger.d/w with tag "ViewerModel"
  </acceptance_criteria>
  <done>All 20 println calls in BookmarkViewerScreenModel.kt are removed (hot-path) or replaced with AppLogger (non-hot-path)</done>
</task>

</tasks>

<verification>
- `grep -c "println" App.kt` returns 0
- `grep -c "println" BookmarkViewerScreenModel.kt` returns 0
- `grep -c "triggerMigration" App.kt` returns >= 1
- `grep -c "AppLogger" App.kt` returns >= 8
- `grep -c "AppLogger" BookmarkViewerScreenModel.kt` returns >= 12
- User confirms build succeeds externally
</verification>

<success_criteria>
- triggerMigration() is called on app startup in a LaunchedEffect — existing DB credentials get promoted to SecureCredentialStore
- Zero println calls remain in App.kt and BookmarkViewerScreenModel.kt
- Hot-path logging (ReadProgressSync, Coil interceptor) is removed entirely
- Non-hot-path logging (refresh, navigation, highlights) uses AppLogger.d/w with appropriate tags
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-wiring-cleanup/07-02-SUMMARY.md`
</output>
