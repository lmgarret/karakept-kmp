---
phase: 03-code-splitting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositoryBatch.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepository.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt
autonomous: true
requirements: [SPLIT-04]

must_haves:
  truths:
    - "BookmarkRepository.kt is under 500 lines"
    - "BookmarkActionsRepository.kt is under 500 lines"
    - "SettingsRepository.kt is under 500 lines"
    - "All repository functionality is preserved — no behavioral changes"
    - "App compiles identically after splitting"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt"
      provides: "Extracted BookmarkSyncPipeline class with SyncConfiguration sealed class"
      min_lines: 400
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositoryBatch.kt"
      provides: "Batch operation extension functions for BookmarkActionsRepository"
      min_lines: 100
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt"
      provides: "All set* mutation methods and layout CRUD as extension functions"
      min_lines: 200
  key_links:
    - from: "BookmarkRepository.kt"
      to: "BookmarkSyncPipeline.kt"
      via: "instantiation of BookmarkSyncPipeline"
      pattern: "BookmarkSyncPipeline\\("
    - from: "BookmarkActionsRepositoryBatch.kt"
      to: "BookmarkActionsRepository"
      via: "extension functions on BookmarkActionsRepository"
      pattern: "fun BookmarkActionsRepository\\."
    - from: "SettingsRepositoryMutations.kt"
      to: "SettingsRepository"
      via: "extension functions on SettingsRepository"
      pattern: "fun SettingsRepository\\."
    - from: "AppModule.kt"
      to: "BookmarkRepository.kt"
      via: "Koin DI wiring — constructor signature must not change"
      pattern: "single.*BookmarkRepository\\("
---

<objective>
Split the three largest repository files (BookmarkRepository, BookmarkActionsRepository, SettingsRepository) into focused, single-responsibility modules under 500 lines each.

Purpose: Reduce file size from ~1000 lines each to under 500 lines, improving readability and enabling independent testing in Phase 4.
Output: 3 new extracted files + 3 trimmed original files, all under 500 lines.
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-splitting/03-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract BookmarkSyncPipeline from BookmarkRepository</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/di/AppModule.kt
  </read_first>
  <action>
Extract the `BookmarkSyncPipeline` inner class (approx lines 353-830) from BookmarkRepository.kt into a new top-level file `BookmarkSyncPipeline.kt` in the same package (`com.karakept.app.data.repository`).

Steps:
1. Read BookmarkRepository.kt completely. Identify the `BookmarkSyncPipeline` inner class, the `SyncConfiguration` sealed class, and the `executeSyncPipeline` function.
2. Create `BookmarkSyncPipeline.kt` containing:
   - The `SyncConfiguration` sealed class hierarchy (Full, Filtered, ForList) and `ApiFilters` data class — move from BookmarkRepository.kt
   - The `BookmarkSyncPipeline` class converted from `inner class` to a standalone `internal class`
   - All outer-class members accessed via `this@BookmarkRepository` must become constructor parameters: `bookmarkDao`, `remoteDataSource`, `bookmarkActionsRepository`, `settingsRepository`, `highlightRepository`, `imageCacheManager`, `syncProgress` (MutableStateFlow), and any other members referenced
   - The `executeSyncPipeline` helper and `cacheHeroAssetsForBookmark` function if they belong to the pipeline
   - Keep the same package: `package com.karakept.app.data.repository`
3. Update BookmarkRepository.kt:
   - Remove the `BookmarkSyncPipeline` inner class, `SyncConfiguration` sealed class, and moved functions
   - Where BookmarkSyncPipeline was instantiated, create the new class passing explicit dependencies
   - Change any `private` members that BookmarkSyncPipeline needs to `internal`
   - Verify that `SyncConfiguration` is still importable from the same package (no import changes needed for callers)
4. Do NOT change the BookmarkRepository constructor signature — the Koin `single { BookmarkRepository(get(), get(), ...) }` in AppModule.kt must continue working unchanged.
5. Do NOT change any behavioral logic — this is a pure structural move.

Target: BookmarkRepository.kt should drop to ~350 lines. BookmarkSyncPipeline.kt should be ~500 lines or under.
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkRepository.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkSyncPipeline.kt</automated>
  </verify>
  <acceptance_criteria>
    - BookmarkRepository.kt is under 500 lines (target ~350)
    - BookmarkSyncPipeline.kt exists with `package com.karakept.app.data.repository`
    - BookmarkSyncPipeline.kt contains `internal class BookmarkSyncPipeline` (not `inner class`)
    - BookmarkSyncPipeline.kt contains `sealed class SyncConfiguration`
    - BookmarkRepository.kt no longer contains `inner class BookmarkSyncPipeline`
    - BookmarkRepository.kt no longer contains `sealed class SyncConfiguration`
    - BookmarkRepository constructor signature is unchanged
    - No `import.*SyncConfiguration` needed in files that already use it (same package)
  </acceptance_criteria>
  <done>BookmarkSyncPipeline extracted as standalone class, BookmarkRepository.kt under 500 lines, no constructor signature changes</done>
</task>

<task type="auto">
  <name>Task 2: Extract batch operations from BookmarkActionsRepository and mutations from SettingsRepository</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositoryBatch.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepository.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepository.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/di/AppModule.kt
  </read_first>
  <action>
Part A — BookmarkActionsRepository batch extraction:
1. Read BookmarkActionsRepository.kt completely. Identify all `batch*` methods (approx lines 411-575): `batchArchive`, `batchDelete`, `batchFavorite`, `batchUpdateTags`, `batchMoveToList`, etc.
2. Create `BookmarkActionsRepositoryBatch.kt` in the same package:
   - Package: `com.karakept.app.data.repository`
   - Define extension functions: `fun BookmarkActionsRepository.batchArchive(...)`, `fun BookmarkActionsRepository.batchDelete(...)`, etc.
   - Each batch method becomes an extension function on `BookmarkActionsRepository`
   - These methods need access to internal members of BookmarkActionsRepository — change required members from `private` to `internal`: at minimum the DAO references, `performAction`/`queueAction` helpers, `remoteDataSource`, and any other members the batch methods call
3. Remove the batch methods from BookmarkActionsRepository.kt.
4. Do NOT change the BookmarkActionsRepository constructor signature.

Target: BookmarkActionsRepository.kt should drop to ~450 lines (under 500).

Part B — SettingsRepository mutations extraction:
1. Read SettingsRepository.kt completely. Identify all `set*` mutation methods (approx lines 592-850) and the update helper functions (approx lines 881-920), plus layout CRUD methods (approx lines 932-988).
2. Create `SettingsRepositoryMutations.kt` in the same package:
   - Package: `com.karakept.app.data.repository`
   - Define extension functions: `fun SettingsRepository.setThemeMode(...)`, `fun SettingsRepository.setAccentColor(...)`, etc.
   - Include all `set*` methods, `updateThemeSettings`/`updateDisplaySettings`/etc. helper functions, and layout CRUD (`saveLayout`, `deleteLayout`, `setDefaultLayout`, `setListLayout`)
   - Change required SettingsRepository members from `private` to `internal`: the DataStore reference, any private helper functions called by set* methods
3. Remove the extracted methods from SettingsRepository.kt.
4. Do NOT change the SettingsRepository constructor signature.

Target: SettingsRepository.kt should drop to ~400 lines (under 500).
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepository.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/BookmarkActionsRepositoryBatch.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepository.kt composeApp/src/commonMain/kotlin/com/karakept/app/data/repository/SettingsRepositoryMutations.kt</automated>
  </verify>
  <acceptance_criteria>
    - BookmarkActionsRepository.kt is under 500 lines (target ~450)
    - BookmarkActionsRepositoryBatch.kt exists with `package com.karakept.app.data.repository`
    - BookmarkActionsRepositoryBatch.kt contains `fun BookmarkActionsRepository.batch` (extension functions)
    - SettingsRepository.kt is under 500 lines (target ~400)
    - SettingsRepositoryMutations.kt exists with `package com.karakept.app.data.repository`
    - SettingsRepositoryMutations.kt contains `fun SettingsRepository.set` (extension functions)
    - SettingsRepositoryMutations.kt contains layout CRUD: `fun SettingsRepository.saveLayout`
    - Neither repository's constructor signature changed
    - No new imports needed in calling code (same package, extension functions resolve automatically)
  </acceptance_criteria>
  <done>Batch operations extracted from BookmarkActionsRepository, mutations extracted from SettingsRepository, both files under 500 lines</done>
</task>

</tasks>

<verification>
1. All 6 repository files (3 original + 3 new) have `package com.karakept.app.data.repository`
2. No original file exceeds 500 lines: `wc -l` on BookmarkRepository.kt, BookmarkActionsRepository.kt, SettingsRepository.kt
3. No constructor signatures changed — verify AppModule.kt Koin bindings still match
4. The `setBookmarkRepository()` circular dependency wiring in AppModule.kt is unaffected
5. User confirms clean build (build runs externally)
</verification>

<success_criteria>
- BookmarkRepository.kt under 500 lines with BookmarkSyncPipeline extracted
- BookmarkActionsRepository.kt under 500 lines with batch operations extracted
- SettingsRepository.kt under 500 lines with mutation methods extracted
- 3 new focused files created, all in same package
- No behavioral changes — pure structural refactoring
- App compiles and functions identically (verified by user build)
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-splitting/03-01-SUMMARY.md`
</output>
