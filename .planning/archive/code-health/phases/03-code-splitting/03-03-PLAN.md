---
phase: 03-code-splitting
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreen.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenDialogs.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenScrollAction.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenExpandedLayout.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreen.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerContent.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerSnackbar.kt
autonomous: true
requirements: [SPLIT-01, SPLIT-03]

must_haves:
  truths:
    - "MainScreen.kt is under 500 lines"
    - "BookmarkViewerScreen.kt is under 500 lines"
    - "Dialogs, scroll-action logic, and expanded layout are in separate files"
    - "BookmarkViewerContent composable is in its own file"
    - "All UI functionality is preserved — no visual or behavioral changes"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenDialogs.kt"
      provides: "RenameListDialog, batch dialogs, filter overlays, add bookmark dialog composables"
      min_lines: 100
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenScrollAction.kt"
      provides: "Scroll-action LaunchedEffect logic as composable or utility function"
      min_lines: 100
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenExpandedLayout.kt"
      provides: "3-column expanded layout composable"
      min_lines: 150
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerContent.kt"
      provides: "BookmarkViewerContent composable extracted from BookmarkViewerScreen.kt"
      min_lines: 300
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerSnackbar.kt"
      provides: "Snackbar utility composables (rememberSnackbarHostStateWithDelay, showSnackbarEvent)"
      min_lines: 30
  key_links:
    - from: "MainScreen.kt"
      to: "main/MainScreenDialogs.kt"
      via: "composable function calls from Content()"
      pattern: "RenameListDialog\\(|BatchDeleteConfirmDialog\\("
    - from: "MainScreen.kt"
      to: "main/MainScreenScrollAction.kt"
      via: "composable function call or LaunchedEffect"
      pattern: "MainScreenScrollAction\\(|ScrollActionEffect\\("
    - from: "MainScreen.kt"
      to: "main/MainScreenExpandedLayout.kt"
      via: "composable function call for wide layout"
      pattern: "MainScreenExpandedLayout\\("
    - from: "BookmarkViewerScreen.kt"
      to: "BookmarkViewerContent.kt"
      via: "composable function call from Screen.Content()"
      pattern: "BookmarkViewerContent\\("
    - from: "BookmarkViewerContent.kt"
      to: "viewer/ViewerSnackbar.kt"
      via: "snackbar utility calls"
      pattern: "rememberSnackbarHostStateWithDelay\\(|showSnackbarEvent\\("
---

<objective>
Split MainScreen.kt (1321 lines) and BookmarkViewerScreen.kt (1045 lines) into focused composable files, bringing each under 500 lines.

Purpose: Extract dialogs, layout variants, scroll-action logic, and viewer content into separate files for readability and independent maintenance.
Output: 5 new composable files + 2 trimmed original files, all under 500 lines.
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-splitting/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MainScreen dialogs, scroll-action, and expanded layout</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreen.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenDialogs.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenScrollAction.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenExpandedLayout.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreen.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/BookmarkListContent.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenTopBar.kt
  </read_first>
  <action>
Read MainScreen.kt completely. Identify the following sections for extraction:

**File 1 — MainScreenScrollAction.kt** (package: `com.karakept.app.ui.screens.main`):
Extract the scroll-action LaunchedEffect block (approx lines 255-419, ~165 lines). This is a self-contained block that handles scroll-to-action behavior.
- Create a composable function: `@Composable fun MainScreenScrollAction(...)` that encapsulates this LaunchedEffect logic
- Pass all needed state as parameters (lazy list state, scroll action index, bookmark list, etc.)
- Or if the logic is non-composable, extract as a utility function called from within the LaunchedEffect
- The key is to move the bulk of the code out of MainScreen.kt's Content() function

**File 2 — MainScreenExpandedLayout.kt** (package: `com.karakept.app.ui.screens.main`):
Extract the expanded 3-column layout composable (approx lines 768-1019, ~250 lines).
- Create: `@Composable fun MainScreenExpandedLayout(...)`
- Pass all needed callbacks and state as parameters
- This is the wide-screen layout variant used when window width supports 3 columns

**File 3 — MainScreenDialogs.kt** (package: `com.karakept.app.ui.screens.main`):
Extract dialogs and overlays (approx lines 1077-1321, ~240 lines):
- `RenameListDialog` composable
- Any batch confirmation dialogs
- Add bookmark dialog/overlay
- Filter action overlays
- `rememberSnackbarHostState` helper if it exists in MainScreen.kt
- Group all dialog/overlay composables into this single file

**After extraction:**
- Update MainScreen.kt to call the extracted composables, passing required state and callbacks as parameters
- Add necessary imports to MainScreen.kt for the new `main.*` composables
- Verify no composable references are broken — all functions from the same `main` package do not need explicit imports if they are in the same package. But `main` is a sub-package of `screens`, so MainScreen.kt (in `screens`) WILL need imports from `screens.main.*`
- Remove extracted code from MainScreen.kt

Target: MainScreen.kt under 500 lines (target ~300).
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreen.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenDialogs.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenScrollAction.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/main/MainScreenExpandedLayout.kt</automated>
  </verify>
  <acceptance_criteria>
    - MainScreen.kt is under 500 lines (target ~300)
    - MainScreenDialogs.kt exists with `package com.karakept.app.ui.screens.main`
    - MainScreenDialogs.kt contains `@Composable` and `fun RenameListDialog`
    - MainScreenScrollAction.kt exists with `package com.karakept.app.ui.screens.main`
    - MainScreenScrollAction.kt contains `@Composable` annotation
    - MainScreenExpandedLayout.kt exists with `package com.karakept.app.ui.screens.main`
    - MainScreenExpandedLayout.kt contains `@Composable` and `fun MainScreenExpandedLayout`
    - MainScreen.kt imports from `com.karakept.app.ui.screens.main.*` for extracted composables
    - MainScreen.kt calls the extracted composable functions (not inlining the old code)
    - No file in main/ exceeds 500 lines
  </acceptance_criteria>
  <done>MainScreen.kt under 500 lines with dialogs, scroll-action, and expanded layout extracted to main/ package</done>
</task>

<task type="auto">
  <name>Task 2: Extract BookmarkViewerContent and snackbar utilities from BookmarkViewerScreen</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreen.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerContent.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerSnackbar.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreen.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerScrollBehavior.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerTopBar.kt
  </read_first>
  <action>
Read BookmarkViewerScreen.kt completely. Identify sections for extraction.

**File 1 — BookmarkViewerContent.kt** (package: `com.karakept.app.ui.screens`):
Extract the `BookmarkViewerContent` composable (approx lines 102-967, ~865 lines) into its own file.
- Move the entire `@Composable fun BookmarkViewerContent(...)` function
- Keep it in `com.karakept.app.ui.screens` package (same level as BookmarkViewerScreen.kt, NOT in viewer/)
- BookmarkViewerScreen.kt's `Content()` method should call `BookmarkViewerContent(...)`
- All imports used by BookmarkViewerContent move to the new file
- BookmarkViewerScreen.kt retains only the Screen data class with its `Content()` override

Note: BookmarkViewerContent.kt may be ~400-500 lines after extraction. That is acceptable as it is under 500. If it exceeds 500, extract the highlight-handling logic (highlight selection, scroll-to-highlight LaunchedEffects, ~100 lines) into `viewer/ViewerHighlightHandling.kt`.

**File 2 — ViewerSnackbar.kt** (package: `com.karakept.app.ui.screens.viewer`):
Extract snackbar utilities (approx lines 968-1045):
- `rememberSnackbarHostStateWithDelay` composable
- `showSnackbarEvent` helper function
- These are reusable utilities that belong in the viewer/ package

**After extraction:**
- BookmarkViewerScreen.kt should contain only the Screen data class (~100 lines)
- Update imports: BookmarkViewerScreen.kt needs to import `BookmarkViewerContent` (same package, so no import needed)
- BookmarkViewerContent.kt needs to import snackbar utilities from `viewer.ViewerSnackbar`

Target: BookmarkViewerScreen.kt ~100 lines. BookmarkViewerContent.kt ~400-500 lines.
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerScreen.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/BookmarkViewerContent.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/viewer/ViewerSnackbar.kt</automated>
  </verify>
  <acceptance_criteria>
    - BookmarkViewerScreen.kt is under 500 lines (target ~100)
    - BookmarkViewerContent.kt exists with `package com.karakept.app.ui.screens`
    - BookmarkViewerContent.kt contains `@Composable` and `fun BookmarkViewerContent`
    - BookmarkViewerContent.kt is under 500 lines
    - ViewerSnackbar.kt exists with `package com.karakept.app.ui.screens.viewer`
    - ViewerSnackbar.kt contains `fun rememberSnackbarHostStateWithDelay`
    - ViewerSnackbar.kt contains `fun showSnackbarEvent`
    - BookmarkViewerScreen.kt still contains `data class BookmarkViewerScreen` (the Screen object)
    - BookmarkViewerScreen.kt no longer contains `fun BookmarkViewerContent` (moved out)
    - No file exceeds 500 lines
  </acceptance_criteria>
  <done>BookmarkViewerScreen.kt under 500 lines with content and snackbar utilities extracted</done>
</task>

</tasks>

<verification>
1. MainScreen.kt under 500 lines: `wc -l MainScreen.kt`
2. BookmarkViewerScreen.kt under 500 lines: `wc -l BookmarkViewerScreen.kt`
3. No new file exceeds 500 lines
4. All new files in correct packages (main/ for MainScreen extractions, screens/ for BookmarkViewerContent, viewer/ for snackbar)
5. User confirms clean build and UI functions identically
</verification>

<success_criteria>
- MainScreen.kt under 500 lines (target ~300) with 3 composable files extracted to main/
- BookmarkViewerScreen.kt under 500 lines (target ~100) with content extracted
- BookmarkViewerContent.kt under 500 lines
- 5 new composable files created, all under 500 lines
- No visual or behavioral changes — pure structural refactoring
- App compiles and functions identically (verified by user build)
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-splitting/03-03-SUMMARY.md`
</output>
