---
phase: 03-code-splitting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelActions.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelBatch.kt
  - composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelPagination.kt
autonomous: true
requirements: [SPLIT-02]

must_haves:
  truths:
    - "MainScreenModel.kt is under 500 lines"
    - "Pagination, actions, and batch/selection concerns are in separate files"
    - "Extension functions can access shared state via internal visibility"
    - "screenModelScope is accessible from extension function files"
    - "All MainScreenModel functionality is preserved — no behavioral changes"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelActions.kt"
      provides: "Individual bookmark action extension functions (toggle*, delete, update, move, add/remove tag, create bookmark, scroll action)"
      min_lines: 150
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelBatch.kt"
      provides: "Selection and batch operation extension functions (batch*, select*, range*)"
      min_lines: 150
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelPagination.kt"
      provides: "Pagination extension functions (loadBookmarksPage, resetPagination, loadNextPage, findPageWithItems)"
      min_lines: 80
  key_links:
    - from: "MainScreenModelActions.kt"
      to: "MainScreenModel"
      via: "extension functions accessing internal state"
      pattern: "fun MainScreenModel\\."
    - from: "MainScreenModelBatch.kt"
      to: "MainScreenModel"
      via: "extension functions accessing internal state"
      pattern: "fun MainScreenModel\\."
    - from: "MainScreenModelPagination.kt"
      to: "MainScreenModel"
      via: "extension functions accessing internal state"
      pattern: "fun MainScreenModel\\."
    - from: "MainScreenModelActions.kt"
      to: "screenModelScope"
      via: "Voyager public extension property import"
      pattern: "import cafe.adriel.voyager.core.model.screenModelScope"
---

<objective>
Split MainScreenModel.kt (1084 lines) into focused concern files using Kotlin extension functions, bringing the main file under 500 lines.

Purpose: Separate pagination, individual actions, and batch/selection concerns into their own files for readability and future testability.
Output: 3 new extension function files + trimmed MainScreenModel.kt, all under 500 lines.
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-splitting/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change private members to internal and extract pagination extension functions</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelPagination.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/di/AppModule.kt
  </read_first>
  <action>
Step 1 — Visibility changes in MainScreenModel.kt:
Change the following members from `private` to `internal` so extension functions in other files can access them. Do this for ALL members that will be needed by the 3 extension function files (actions, batch, pagination):

Mutable state flows:
- `_accumulatedBookmarks` -> `internal val _accumulatedBookmarks`
- `_selectedBookmarkIds` -> `internal val _selectedBookmarkIds`
- `_currentFilter` -> `internal val _currentFilter`
- `_isSyncing` -> `internal val _isSyncing`
- `_scrollToTopTrigger` -> `internal val _scrollToTopTrigger`
- `_pendingBookmarks` -> `internal val _pendingBookmarks`
- `_createBookmarkResult` -> `internal val _createBookmarkResult`
- `_selectedServer` -> `internal val _selectedServer`
- `_bookmarkListVersion` -> `internal val _bookmarkListVersion`
- `_currentListContext` -> `internal val _currentListContext`
- `_hasMoreItems` -> `internal val _hasMoreItems`
- `_isLoadingMore` -> `internal val _isLoadingMore`
- `_currentPage` -> `internal val _currentPage`

Helper functions:
- `updateAccumulatedBookmarks` -> `internal suspend fun updateAccumulatedBookmarks`

Repository/controller references (if private):
- `bookmarkActionsRepository` -> `internal`
- `bookmarkRepository` -> `internal`
- `settingsRepository` -> `internal`
- `listRepository` -> `internal`
- `bookmarkActionController` -> `internal`

Mutex:
- `bookmarksMutex` -> `internal val bookmarksMutex`

Read each member declaration carefully — only change visibility for members that are actually `private`. Some may already be non-private.

Step 2 — Create MainScreenModelPagination.kt:
Package: `com.karakept.app.ui.screens`

Extract these functions from MainScreenModel.kt as extension functions:
- `loadBookmarksPage` -> `fun MainScreenModel.loadBookmarksPage(...)`
- `resetPagination` -> `fun MainScreenModel.resetPagination(...)`
- `loadNextPage` -> `fun MainScreenModel.loadNextPage()`
- `findPageWithItems` -> `fun MainScreenModel.findPageWithItems(...)`

Each function accesses `screenModelScope` — import it: `import cafe.adriel.voyager.core.model.screenModelScope`

Add file-level KDoc: `/** Pagination extension functions for MainScreenModel. */`

Remove the extracted functions from MainScreenModel.kt.

Do NOT change the MainScreenModel constructor signature. The Koin factory binding must continue working.
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelPagination.kt</automated>
  </verify>
  <acceptance_criteria>
    - MainScreenModelPagination.kt exists with `package com.karakept.app.ui.screens`
    - MainScreenModelPagination.kt contains `fun MainScreenModel.loadNextPage`
    - MainScreenModelPagination.kt contains `fun MainScreenModel.loadBookmarksPage`
    - MainScreenModelPagination.kt contains `import cafe.adriel.voyager.core.model.screenModelScope`
    - MainScreenModel.kt contains `internal val _accumulatedBookmarks`
    - MainScreenModel.kt contains `internal val bookmarksMutex`
    - MainScreenModel.kt no longer contains `fun loadNextPage` as a member function (only exists in pagination file)
    - MainScreenModel constructor signature is unchanged
  </acceptance_criteria>
  <done>Shared state changed to internal visibility, pagination functions extracted to MainScreenModelPagination.kt</done>
</task>

<task type="auto">
  <name>Task 2: Extract action and batch extension functions</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelActions.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelBatch.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelPagination.kt
  </read_first>
  <action>
Part A — Create MainScreenModelActions.kt:
Package: `com.karakept.app.ui.screens`

Extract individual bookmark action methods from MainScreenModel.kt as extension functions:
- `toggleBookmarkArchive` -> `fun MainScreenModel.toggleBookmarkArchive(...)`
- `toggleBookmarkFavorite` -> `fun MainScreenModel.toggleBookmarkFavorite(...)`
- `toggleBookmarkRead` -> `fun MainScreenModel.toggleBookmarkRead(...)`
- `deleteBookmark` -> `fun MainScreenModel.deleteBookmark(...)`
- `updateBookmarkTags` -> `fun MainScreenModel.updateBookmarkTags(...)`
- `moveBookmarkToList` -> `fun MainScreenModel.moveBookmarkToList(...)`
- `addTagToBookmark` -> `fun MainScreenModel.addTagToBookmark(...)`
- `removeTagFromBookmark` -> `fun MainScreenModel.removeTagFromBookmark(...)`
- `createBookmark` -> `fun MainScreenModel.createBookmark(...)`
- `handleScrollAction` / `setScrollActionIndex` -> extract any scroll-action-related model methods

Also extract any methods in the ~1005-1084 range that relate to individual actions.

Import `screenModelScope`: `import cafe.adriel.voyager.core.model.screenModelScope`
Add file-level KDoc: `/** Bookmark action extension functions for MainScreenModel. */`

Part B — Create MainScreenModelBatch.kt:
Package: `com.karakept.app.ui.screens`

Extract selection and batch operation methods (approx lines 802-1003):
- `toggleBookmarkSelection` -> `fun MainScreenModel.toggleBookmarkSelection(...)`
- `selectAllBookmarks` -> `fun MainScreenModel.selectAllBookmarks()`
- `clearSelection` -> `fun MainScreenModel.clearSelection()`
- `selectRange` -> `fun MainScreenModel.selectRange(...)`
- `getSelectedBookmarks` -> `fun MainScreenModel.getSelectedBookmarks()`
- `batchArchive` -> `fun MainScreenModel.batchArchive()`
- `batchDelete` -> `fun MainScreenModel.batchDelete()`
- `batchFavorite` -> `fun MainScreenModel.batchFavorite()`
- `batchUpdateTags` -> `fun MainScreenModel.batchUpdateTags(...)`
- `batchMoveToList` -> `fun MainScreenModel.batchMoveToList(...)`
- Any other batch/selection methods in that range

Import `screenModelScope`: `import cafe.adriel.voyager.core.model.screenModelScope`
Add file-level KDoc: `/** Selection and batch operation extension functions for MainScreenModel. */`

Part C — Remove all extracted functions from MainScreenModel.kt.

After removal, MainScreenModel.kt should contain only:
- Class declaration and constructor
- State declarations (MutableStateFlow, derived flows, combine operators)
- Init block with InitState state machine
- Sync, filter, search, and list methods (approx lines 485-614 — these are small, core methods that stay)
- `onDispose` or cleanup methods

Target: MainScreenModel.kt under 500 lines (target ~350).
  </action>
  <verify>
    <automated>wc -l composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModel.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelActions.kt composeApp/src/commonMain/kotlin/com/karakept/app/ui/screens/MainScreenModelBatch.kt</automated>
  </verify>
  <acceptance_criteria>
    - MainScreenModel.kt is under 500 lines (target ~350)
    - MainScreenModelActions.kt exists with `package com.karakept.app.ui.screens`
    - MainScreenModelActions.kt contains `fun MainScreenModel.toggleBookmarkArchive`
    - MainScreenModelActions.kt contains `fun MainScreenModel.deleteBookmark`
    - MainScreenModelActions.kt contains `import cafe.adriel.voyager.core.model.screenModelScope`
    - MainScreenModelBatch.kt exists with `package com.karakept.app.ui.screens`
    - MainScreenModelBatch.kt contains `fun MainScreenModel.batchArchive`
    - MainScreenModelBatch.kt contains `fun MainScreenModel.toggleBookmarkSelection`
    - MainScreenModel.kt still contains `class MainScreenModel` declaration
    - MainScreenModel.kt still contains `sealed class InitState` (or equivalent state machine)
    - MainScreenModel.kt no longer contains `fun toggleBookmarkArchive` as a member
    - MainScreenModel.kt no longer contains `fun batchArchive` as a member
  </acceptance_criteria>
  <done>MainScreenModel.kt under 500 lines with actions and batch concerns extracted to dedicated files</done>
</task>

</tasks>

<verification>
1. MainScreenModel.kt under 500 lines: `wc -l MainScreenModel.kt`
2. All 4 files (1 original + 3 new) in package `com.karakept.app.ui.screens`
3. Extension functions use `fun MainScreenModel.methodName` pattern
4. All new files import `screenModelScope` from Voyager
5. MainScreenModel constructor signature unchanged — Koin factory binding still works
6. User confirms clean build
</verification>

<success_criteria>
- MainScreenModel.kt under 500 lines (target ~350)
- 3 new extension function files created: Actions (~250 lines), Batch (~200 lines), Pagination (~120 lines)
- All extension functions access shared state via `internal` visibility
- `screenModelScope` accessible from all extension function files
- No behavioral changes — pure structural refactoring
- App compiles and functions identically (verified by user build)
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-splitting/03-02-SUMMARY.md`
</output>
