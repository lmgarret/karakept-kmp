---
phase: 05-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/kotlin/com/karakept/app/utils/HtmlArchiveProcessor.kt
  - composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt
  - composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlArchiveProcessorTest.kt
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "HTML rendered in WEB/archive mode has no iframes, object, embed, applet, or form tags"
    - "HtmlArchiveProcessor strips dangerous embedding elements that can load external content"
    - "The misleading 'JavaScript disabled' comment in HtmlRenderer.android.kt is corrected"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/com/karakept/app/utils/HtmlArchiveProcessor.kt"
      provides: "iframe/object/embed/applet/form removal in processForArchive"
      contains: "iframe, object, embed, applet"
    - path: "composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt"
      provides: "Corrected security comment about JS being enabled"
      contains: "JavaScript enabled"
    - path: "composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlArchiveProcessorTest.kt"
      provides: "Tests verifying dangerous element stripping"
      contains: "class HtmlArchiveProcessorTest"
  key_links:
    - from: "composeApp/src/commonMain/kotlin/com/karakept/app/ui/components/HtmlContent.kt"
      to: "HtmlArchiveProcessor.processForArchive"
      via: "function call for ViewerMode.WEB"
      pattern: "HtmlArchiveProcessor\\.processForArchive"
---

<objective>
Harden HtmlArchiveProcessor to strip dangerous embedding elements (iframes, objects, embeds, applets, forms) that can load external content or enable phishing in the Android WebView where JS is enabled. Fix the misleading "JavaScript disabled" comment in HtmlRenderer.android.kt.

Purpose: Prevent XSS and content injection via embedding elements in WEB/archive mode. The READER mode path (HtmlSanitizer with Safelist.relaxed()) already strips these -- only HtmlArchiveProcessor has the gap.
Output: Hardened HtmlArchiveProcessor, corrected comment, unit tests
</objective>

<execution_context>
@/home/lm/.claude/get-shit-done/workflows/execute-plan.md
@/home/lm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-hardening/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dangerous element stripping to HtmlArchiveProcessor and fix JS comment</name>
  <files>
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/HtmlArchiveProcessor.kt,
    composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt,
    composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlArchiveProcessorTest.kt
  </files>
  <read_first>
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/HtmlArchiveProcessor.kt,
    composeApp/src/commonMain/kotlin/com/karakept/app/utils/HtmlSanitizer.kt,
    composeApp/src/androidMain/kotlin/com/karakept/app/ui/components/HtmlRenderer.android.kt,
    composeApp/src/desktopTest/kotlin/com/karakept/app/utils/BackupCryptoTest.kt
  </read_first>
  <behavior>
    - Test: processForArchive strips all iframe tags from HTML input
    - Test: processForArchive strips all object tags from HTML input
    - Test: processForArchive strips all embed tags from HTML input
    - Test: processForArchive strips all applet tags from HTML input
    - Test: processForArchive strips all form tags (and child inputs) from HTML input
    - Test: processForArchive preserves structural HTML (div, p, h1, img, a, table, style) after stripping
    - Test: processForArchive still removes script tags and event handlers (regression)
    - Test: processForArchive returns empty string for null/blank input (regression)
  </behavior>
  <action>
    **Step 1 (RED): Create test file** at `composeApp/src/commonTest/kotlin/com/karakept/app/utils/HtmlArchiveProcessorTest.kt`:
    - Package: `com.karakept.app.utils`
    - Import: `kotlin.test.Test`, `kotlin.test.assertEquals`, `kotlin.test.assertFalse`, `kotlin.test.assertTrue`
    - Class: `class HtmlArchiveProcessorTest`
    - Write tests for each behavior above. For stripping tests, create HTML containing the dangerous element, call `HtmlArchiveProcessor.processForArchive(html)`, and assert the result does NOT contain the element tag. For preservation tests, assert that safe elements (div, p, img, style) remain in the output.
    - Run tests -- they should FAIL for iframe/object/embed/applet/form stripping (these elements are not yet removed by the current code).

    **Step 2 (GREEN): Modify HtmlArchiveProcessor.kt**:
    In `processForArchive()`, after the existing `doc.select("script").remove()` line (line 28), add two new removal blocks:

    ```kotlin
    // Remove dangerous embedding elements that can load external content/JS
    doc.select("iframe, object, embed, applet").remove()

    // Remove forms to prevent phishing attacks
    doc.select("form").remove()
    ```

    Also replace the `println` error logging on line 74 with AppLogger:
    - Add import: `import com.karakept.app.utils.AppLogger` (note: same package, but be explicit)
    - Replace: `println("HtmlArchiveProcessor: ...")` with `AppLogger.e("HtmlArchiveProcessor", "Error processing HTML: ${e.message}", e)`

    **Step 3: Fix misleading comment in HtmlRenderer.android.kt**:
    On line 41, change the comment from:
    ```
     * - JavaScript disabled (always, in both modes)
    ```
    to:
    ```
     * - JavaScript enabled (required for highlight functionality and JS bridge)
    ```

    Run tests again -- all should pass.
  </action>
  <verify>
    <automated>User runs: ./gradlew :composeApp:desktopTest --tests "*.HtmlArchiveProcessorTest"</automated>
  </verify>
  <acceptance_criteria>
    - HtmlArchiveProcessor.kt contains `doc.select("iframe, object, embed, applet").remove()`
    - HtmlArchiveProcessor.kt contains `doc.select("form").remove()`
    - HtmlArchiveProcessor.kt does NOT contain `println(`
    - HtmlArchiveProcessor.kt contains `AppLogger.e("HtmlArchiveProcessor"`
    - HtmlRenderer.android.kt line 41 contains "JavaScript enabled" (not "JavaScript disabled")
    - HtmlArchiveProcessorTest.kt exists and contains at least 6 test functions
    - All tests in HtmlArchiveProcessorTest pass
  </acceptance_criteria>
  <done>HtmlArchiveProcessor strips iframes, objects, embeds, applets, and forms. Misleading JS comment corrected. All tests pass.</done>
</task>

</tasks>

<verification>
- HtmlArchiveProcessor strips all 5 dangerous element types (iframe, object, embed, applet, form)
- Existing script and event handler removal still works (regression tests)
- Safe HTML elements are preserved for visual fidelity
- Comment in HtmlRenderer.android.kt accurately describes JS state
</verification>

<success_criteria>
- All HtmlArchiveProcessorTest tests pass
- No dangerous embedding elements survive processForArchive()
- HtmlRenderer.android.kt comment matches reality (JS enabled, not disabled)
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-hardening/05-01-SUMMARY.md`
</output>
